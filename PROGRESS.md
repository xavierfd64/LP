# PROGRESS

Broad-but-shallow MVP prototype of the Printing Business Management System, built per `BUILD_SPEC.md` (the original prompt).

## Stack decisions

Followed the recommended stack with a couple of environment-driven adjustments:

- **Next.js 16** (App Router, TypeScript, Turbopack) instead of 14 — that's what `create-next-app` shipped; no functional difference for this build.
- **PostgreSQL 16**, running as a local system service in this container (not docker-compose) because no Docker daemon was available in this sandbox. `docker-compose.yml` is still provided and documented in the README for normal local use.
- **Prisma 7** (`prisma-client` generator, not the older `prisma-client-js`) — this is what `prisma init` scaffolds on v7 and requires a driver adapter (`@prisma/adapter-pg`). Generated client lives at `app/generated/prisma` (gitignored, run `npx prisma generate`).
- **NextAuth v5 (Auth.js beta)** with the Credentials provider. Auth config is split into `lib/auth.config.ts` (Edge-safe, no Prisma/bcrypt — used by `proxy.ts`) and `lib/auth.ts` (full config with the Credentials provider — used by server code). This split is required because Next's Edge Middleware/Proxy runtime can't load Prisma's Node-only client.
- Route protection file is named `proxy.ts` (Next 16 renamed the middleware convention to "proxy").
- Hand-rolled a small Tailwind component kit under `components/ui/` (Button, Card, Badge, Input/Select/Textarea, Table, Alert) instead of running the `shadcn` CLI, to avoid an interactive scaffolding step — same visual approach (Tailwind + simple composable primitives), just written by hand.
- File uploads are stored under `public/uploads/` (not a top-level `/uploads/`) so Next's static file server handles them for free without a custom route handler.

## Phases completed

### Phase 1 — Foundation ✅
- Full Prisma schema covering every entity in the spec (User, Customer, Inquiry, Quotation + line items, Order, WorkflowTemplate/Stage, JobOrder, JobOrderStageLog, File, Payment, QCResult, ReworkRecord, InventoryItem, SupplyLot, InventoryMovement, Fulfillment, RewardRule, RewardTransaction, AuditLog).
- Seed script (`prisma/seed.ts`) creates all demo data from Section 10: 1 admin, 2 staff, 2 production, 3 customers (one flagged `isQualifiedForTerms`), 4 workflow templates (Jersey/Tarp/DTF Shirt/Signage) with real stage lists, 6 inquiries, 5 quotations, 3 orders spanning all 4 required JO states (ON_HOLD unpaid, mid-production, QC+rework-in-progress, fulfilled/completed), inventory items+lots (one below reorder threshold), sample files across categories, a reward rule + reward transactions.
- NextAuth credentials login, JWT session carrying `role`, bcrypt password hashing.
- Route protection in `proxy.ts`: unauthenticated → `/login`; wrong role for a gated prefix (`/admin`, `/production`, `/inventory`, `/payments`, `/users`) → redirected to their own home.
- Self-registration for `CUSTOMER` only (`/register`); other roles are created via an Admin screen (Phase 10/11).
- Role-aware shell layout + sidebar nav (`components/layout/`).
- Core cross-cutting libs used by every later phase: `lib/workflow.ts` (payment-gate + stage-advance business rules), `lib/numbering.ts` (order/JO/quote/lot numbering), `lib/audit.ts`, `lib/notify.ts` (stub), `lib/upload.ts`.

### Phase 2 — Core lifecycle ✅
- Inquiries: customer self-submit (or staff on behalf of a customer), list, detail, staff "Close Inquiry" action.
- Quotations: staff creates from an inquiry (or from scratch) with a dynamic multi-line-item form (client-managed rows, total computed live), Draft → Send → Approve/Reject flow. Approving an inquiry-linked quotation flips the inquiry to `QUOTED`.
- Orders: staff creates an order directly or from an approved quotation, choosing `STANDARD_PARTIAL` (with a configurable required-partial %) or `APPROVED_TERMS` (captures who authorized it and why — both logged to the audit trail). Auto-numbered `ORD-YYYY-####`.
- Job Orders: staff adds one or more JOs to an order, each bound to an active Workflow Template; auto-numbered `JO-###` scoped to the order (fixed the schema to make `joNumber` unique per-order rather than globally, since every order legitimately starts its own `JO-001`).
- Order detail page shows payment-term info, a live payment summary (confirmed vs. required-partial vs. total), and the JO list with a "Start Production" action per JO.
- "Start Production" wires straight into `lib/workflow.ts`'s `startProduction()` — this is Rule #1 from Section 7, enforced now rather than deferred to Phase 3, since the gate logic and the JO-creation flow are naturally the same piece of work. Verified end-to-end (Playwright-driven browser test against the running dev server + seeded DB): attempting to start production on an unpaid `STANDARD_PARTIAL` order is blocked with `On hold: requires 1000.00 confirmed partial payment (has 0.00), or an approved payment-terms exception.`, and the JO stays `ON_HOLD`.
- Job Order detail page (baseline version): header info, full stage list from its template with the current stage highlighted, and stage-log history. Stage-advance UI, QC, files, and fulfillment are added in later phases.

### Phase 3 — Payment & terms gate ✅
- `/payments`: staff/admin view of every payment across all orders, with a "Record Payment" form (creates a `CONFIRMED` payment directly) and Confirm/Reject actions for `PENDING` ones.
- Customer-side "Upload Payment Proof" on the order detail page: amount + file upload, creates a `PENDING` payment with the proof file linked (`lib/upload.ts`); staff then confirms or rejects it from `/payments`.
- Order detail page shows full payment history (date, amount, method, status, proof link) alongside the live payment summary from Phase 2.
- Rule #2 (Section 7) — a JobOrder cannot be `RELEASED` without full payment or an authorized release exception — implemented as `assertCanRelease()` in `lib/workflow.ts` and wired to a "Release" action (shown once a JO reaches `READY`, from `/orders/[id]` and `/job-orders/[id]`).
- "Grant Release Exception" form on the order page captures who authorized it and why (mirrors the approved-terms exception from Phase 2), audit-logged as `RELEASE_EXCEPTION_GRANTED`.
- Verified end-to-end against the seeded DB (Playwright-driven browser runs, cross-checked against server logs and direct DB queries, then reverted the test mutations):
  - Recording a `CASH`/etc. payment that meets the required partial % correctly unblocks "Start Production" (`ON_HOLD` → `IN_PROGRESS`).
  - Attempting to release a `READY` JO on an order that's only 50% paid is blocked with `Cannot release: full payment required (7500.00 of 15000.00 confirmed), or an authorized release exception.` and the JO stays `READY`.
  - Granting a release exception (audit-logged) immediately unblocks the same JO's release (`READY` → `RELEASED`), and the audit trail records both `RELEASE_EXCEPTION_GRANTED` and `JOB_ORDER_RELEASED`.

### Phase 4 — Workflow templates + Production portal ✅
- Admin CRUD for Workflow Templates at `/admin/workflow-templates`: create/edit a template's name and its ordered stage list (add/remove/reorder rows client-side, one stage flagged as the QC stage, any stage flaggable as the install stage), plus an active/inactive toggle. New product types are addable with zero code changes, per spec.
- Production queue at `/production`: every JobOrder currently `IN_PROGRESS`, `REWORK`, or `QC`, sorted by deadline, showing product/qty/customer/deadline/current stage. `QC`-status JOs link to the job order detail page (QC recording UI lands in Phase 5); everything else gets `READY → IN_PROGRESS → COMPLETED` stage actions inline.
- Stage advancement is driven entirely by `lib/workflow.ts`'s `completeCurrentStage()` (built in Phase 1, now wired to real UI): it always advances exactly one stage per the template's order (Rule #4), routes into `QC` status automatically when the next stage is the QC stage, and flips the JO to `READY` when the last stage completes.
- Verified end-to-end against the seeded DB: completing the seeded order's `Pressing` stage (the stage right before `QC` in the DTF Shirt template) correctly moved the JO to `status=QC`, `currentStageOrder=4`, and opened a fresh `QC` stage log in `READY` — confirmed via the DB and the audit trail, then reverted so the seed data matches its documented starting state.

### Phase 5 — QC & Rework ✅
- QC recording form on the job order detail page, shown whenever a JO is at `status=QC`: Pass/Fail toggle, quantity checked/failed, defect notes, and (on Fail) a "route rework to stage" selector defaulting to the stage immediately before QC.
- `recordQCResult()` in `lib/workflow.ts`: on Fail, always creates a `ReworkRecord` and routes the JO's `currentStageOrder` back to the chosen stage with a fresh `READY` stage log, setting `status=REWORK` — this is Rule #3 from Section 7, and it blocks all forward progress on the JO until that stage is redone. On Pass, it closes the QC stage log and advances exactly one stage (or to `READY` for fulfillment if QC was the last stage), reusing the same strict-order logic as regular stage completion (Rule #4).
- Completing a rework stage (via the normal production-queue stage actions) re-enters QC automatically — `completeCurrentStage()` detects `status=REWORK`, closes the open `ReworkRecord`(s) as `DONE`, and opens a new QC stage log — so the QC↔rework loop can repeat, and every attempt stays in history rather than being overwritten (both stage logs and all `QCResult` rows are kept and shown on the JO detail page's "QC history" section, each with its linked rework record if it failed).
- Fixed a seed-data/business-logic mismatch this phase surfaced: the seeded "QC + rework in progress" job order had `currentStageOrder` pointing at the QC stage while its `ReworkRecord.assignedStage` said "Pressing" — inconsistent with how `recordQCResult()` actually routes rework. Corrected the seed so `currentStageOrder` follows the assigned stage and the in-progress rework attempt has its own active stage log, matching what the real flow produces.
- Verified end-to-end against the seeded DB, in both directions: completing the seeded in-progress rework (Pressing) correctly closed the `ReworkRecord`, re-entered QC, and a subsequent Pass advanced the JO to the next stage (`Packing`) with full stage-log history preserved (`STAGE_COMPLETED` → `REWORK_CLOSED` in the audit log). Separately, driving a fresh JO through Pressing → QC → **Fail** correctly created a new `OPEN` `ReworkRecord` (assigned stage `Pressing`, qty 5) and set the JO back to `REWORK` at `currentStageOrder=3`, blocking it from progressing. Reset the dev DB to a clean reseed afterward so demo data matches the documented seed state.

### Phase 6 — Inventory & Supply Lots ✅
- `/inventory`: item list with current stock, reorder threshold, a low-stock banner, an "Add Item" form, and a recent-consumption-by-JO report pulling from `InventoryMovement`.
- `/inventory/[itemId]`: per-item supply lots (lot code, received date/qty/supplier, remaining qty), a "Receive New Lot" form that auto-generates the lot code via `lib/numbering.ts`'s `nextLotCode()` (pattern `{itemShortCode}-{YYYYMM}-{sequence}`, e.g. `DTF-202608-002`) and creates a `RECEIVE` movement, and per-lot "Record Movement" forms covering Allocate/Consume/Reject/Waste (always against a job order for the first two) and Adjust (a signed correction).
- All movements are stored as a signed `qty` delta against the lot (`RECEIVE`/positive-`ADJUST` increase, `ALLOCATE`/`CONSUME`/`REJECT`/`WASTE`/negative-`ADJUST` decrease), applied atomically to both `SupplyLot.remainingQty` and the parent `InventoryItem.currentQty` in `app/actions/inventory.ts`.
- Rule #5 (Section 7) — `remainingQty` must never go negative — enforced by computing the new remaining quantity before writing anything and returning a clear validation error (no DB write at all) if it would go negative.
- Verified end-to-end against the seeded DB: consuming 5 units against a 12-unit lot correctly brought it to 7, and immediately attempting to consume 100 more was rejected with `Cannot consume more than the remaining quantity in this lot (7 available)` — confirmed the lot's `remainingQty` was untouched and no `InventoryMovement` row was written for the rejected attempt.
- Fixed a related seed inconsistency this phase surfaced: the seed had created an `ALLOCATE` movement without actually decrementing the lot's `remainingQty`/item's `currentQty` (a leftover from before movements had a defined signed-delta convention) — corrected the seed data to be internally consistent with how `recordMovementAction` actually accounts for stock.

### Phase 7 — File Repository ✅
- Files tab on the job order detail page, grouped exactly in spec order: Customer Files → Design Versions → Approved Design → Production Files → QC Evidence. Each file shows filename, version, uploader, timestamp, an "Approved / In Use" badge, and a download link (served straight from `public/uploads/` via Next's static file handling).
- Upload form (`app/actions/files.ts`): version auto-increments per job-order-per-category; customers are restricted to uploading `CUSTOMER_FILE`s, staff/admin/production can upload to any category.
- "Approve" action flags a file `isApproved` and simultaneously un-approves any other file in the same JO+category (so exactly one version per category is ever flagged as the active/in-use one). Staff/admin/production can approve any category; customers can only approve `DESIGN_DRAFT` files — this is the design-approval action from spec 5.13 ("design approval action if a design is pending their review").
- Verified end-to-end against the seeded DB: uploaded a new Design Draft v2 (auto-versioned correctly above the existing v1), then approved it as the customer — confirmed only that file flips to `isApproved=true` and no other category's approval state is disturbed. Tightened the customer-approval permission during testing (initially it let a customer approve any category, which doesn't match the spec's design-review framing) and re-verified the fix.

### Phase 8 — Fulfillment ✅
- Fulfillment can only be scheduled once a JO is `RELEASED` (i.e. Rule #2's payment gate has already passed) — a "Schedule Fulfillment" form on the job order detail page offers Pickup, Delivery, or Installation, with Installation only available when the JO's workflow template actually has a stage flagged `isInstallStage` (Signage does; Jersey/Tarp/DTF Shirt don't).
- Pickup: single "Mark Picked Up" action → `RECEIVED`. Delivery: `BOOKED → IN_TRANSIT → DELIVERED` with courier/tracking-number fields and a proof-of-delivery upload. Installation: scheduled date + "Mark Installed" → `INSTALLED`.
- Reaching any terminal fulfillment status marks the JobOrder `COMPLETED`; once every JO on an Order is `COMPLETED`, the Order itself flips to `COMPLETED` and triggers `lib/rewards.ts`'s `onOrderCompleted()` — this pulls Phase 9's core "auto-earn on completion" logic forward since it's the natural trigger point, so Phase 9 is now mostly the reward-rule admin UI and the customer-facing rewards view.
- Verified end-to-end against the seeded DB: released a JO, scheduled a Delivery fulfillment with courier/tracking, walked it through Booked → In Transit → Delivered, and confirmed the full cascade fired correctly — JobOrder → `COMPLETED`, Order → `COMPLETED`, a `RewardTransaction` (`EARN`, 125 points on a ₱12,500 order at the seeded 1pt/₱100 rule) created, and the customer's `rewardPointsBalance` incremented to match.

### Phase 9 — Rewards ✅
- Admin `/admin/rewards`: create reward rules (name, points earned per currency unit, e.g. "1 point per ₱100"), and toggle one active — activating a rule automatically deactivates every other one, so exactly one rule is ever in effect (enforced in `toggleRewardRuleAction`, not just the UI).
- The earn side was already wired in Phase 8 (`lib/rewards.ts`'s `onOrderCompleted()`, called when an Order's last JO completes) — this phase is the admin config UI plus the customer-facing side.
- Customer `/account/rewards`: points balance, full transaction history (EARN in green, REDEEM in slate, signed point deltas), and a "Redeem Points" form (points + free-text "what for") that validates the customer isn't redeeming more than their current balance before writing anything.
- Verified end-to-end against the seeded DB: attempting to redeem 9999 points against a 400-point balance was correctly rejected with no transaction written; redeeming a valid 100 points immediately updated the balance to 300 and appended a `REDEEM` row. Separately, creating and activating a new reward rule from the admin UI correctly flipped the previously-active rule to inactive in the same action.

### Phase 10 — Management Dashboard + Audit Trail ✅
- `/admin/users`: the "Create User" admin screen called for in Section 3 (Staff/Production/Admin accounts are admin-created; only Customer self-registers) — name/email/role/temp-password form, audit-logged as `USER_CREATED`.
- `/admin/dashboard` rebuilt with live widgets, all pulled from real DB queries (no mock data): open quotations, JOs-by-stage breakdown, QC pass/fail rate, low-stock item count, total outstanding balance (with a drill-down list of which orders owe what), upcoming fulfillments (next 8 by scheduled date), new vs. returning customers this month, and reward points issued/redeemed this month.
- `/admin/audit-log`: filterable table (entity type, actor, date range) over every `AuditLog` row written by the app so far — quotation approvals, payment recording, payment-terms/release exceptions, JO stage changes, QC results, rework created/closed, inventory movements, file uploads/approvals, fulfillment/release/completion events, and now user creation, satisfying every state-changing action listed in spec Section 5.12.
- Verified end-to-end: confirmed a fresh reseed has zero audit rows (the seed script doesn't route through the app, so this is correct, not a bug), then created a user through the admin UI and confirmed it appeared in the audit log both unfiltered and when filtering by `entityType=User`.

### Phase 11 — Customer Portal polish ✅
- Rebuilt `/dashboard` for the `CUSTOMER` role into a real dashboard per spec 5.13, pulling live data instead of the generic quick-link grid still used for staff: quotations awaiting approval, active orders count, balance due, reward points — all as clickable stat cards — plus an active-orders list with inline JO-level status chips, recent inquiries, and quotations awaiting approval.
- Added a "Fulfillment & Tracking" section directly to the order detail page (previously tracking/courier info was only visible one level down on the job order detail page) — method, status, scheduled date, and courier/tracking number per JO's fulfillment record.
- Confirmed every customer-facing action from spec 5.13 is reachable: submit inquiry, view/approve quotations, view order/JO progress, upload payment proof, view payment history, approve a pending design draft, view fulfillment/tracking, view rewards balance/history/redeem — all exercised via the earlier phases' end-to-end tests plus a fresh pass over the customer dashboard and order detail page against the seeded DB.

### Phase 12 — Verify business rules ✅

Manual test checklist for every rule in spec Section 7, each one actually driven through the running app against the seeded/reset dev DB (not just read from the code) and then reverted so demo data stays clean:

| # | Rule | How it's enforced | Verified |
|---|---|---|---|
| 1 | JO can't leave `ON_HOLD` without partial payment or an approved-terms exception | `lib/workflow.ts` `assertCanStartProduction()`, called from `startProduction()` | Unpaid standard-partial order: "Start Production" blocked with `On hold: requires 1000.00 confirmed partial payment (has 0.00)...`. Recording a payment that meets the required % immediately unblocks it. |
| 2 | JO can't be `RELEASED` without full payment or an authorized release exception | `assertCanRelease()`, called from `releaseJobOrderAction` | A `READY` JO on a 50%-paid order: release blocked with `Cannot release: full payment required (7500.00 of 15000.00 confirmed)...`. Granting a release exception (audit-logged) immediately unblocks it. |
| 3 | Failed QC always creates a `ReworkRecord` and blocks forward progress | `recordQCResult()` | A QC Fail created a new `OPEN` `ReworkRecord` and routed the JO back to the assigned stage (`status=REWORK`), blocking it from re-entering QC until that stage is redone. |
| 4 | Stage completion always advances exactly one step — never skips | `completeCurrentStage()`'s `stageOrder !== jo.currentStageOrder` guard | Direct test: fabricated an out-of-sequence stage log and confirmed `completeCurrentStage()` throws `This stage is not the job order's current stage.`; completing the real current stage advanced `currentStageOrder` by exactly 1. |
| 5 | `SupplyLot.remainingQty` must never go negative | `recordMovementAction`'s pre-write remaining-quantity check | Consuming 5 of 12 units succeeded (→7); immediately attempting to consume 100 more was rejected with no DB write, `remainingQty` unchanged at 7. |
| 6 | Every state-changing action from Section 5.12 writes an `AuditLog` entry | `logAudit()` called from every mutating server action | Grepped every action file for `logAudit` calls and cross-checked against the Section 5.12 list; found and fixed one gap — marking a stage `IN_PROGRESS` (start-of-stage) wasn't logged, only completion was. Added `STAGE_STATUS_UPDATED` logging to `setStageLogStatus()`. |

Also did a full route crawl (every nav link for all 4 roles, plus a sample of detail pages — orders, inventory items, job orders, workflow templates) confirming `npm run dev` serves every page with no 500s or thrown errors, per the Section 9 phase-closing requirement.

## Known Stubs

- **SMS/Email notifications** — `lib/notify.ts` just does `console.log('[STUB NOTIFY] ...')`. A real build would wire this to Twilio/SendGrid.
- **Payment gateway** — payments are manually recorded by staff or uploaded as proof by the customer; nothing talks to an actual payment processor.
- **Password reset / email verification** — not implemented.
- **Courier tracking** — tracking number/courier are free-text fields, no live courier API integration.
- **Invoice/PDF generation** — not built (spec marks this optional).
- **Object storage** — files are written to local disk (`public/uploads/`) with metadata in the `File` table; a production deployment would swap this for S3-compatible storage behind the same `lib/upload.ts` interface.
- **Single active reward rule** — the schema allows multiple `RewardRule` rows, but only one is ever "active" at a time (activating one deactivates the rest) to keep the earn calculation unambiguous, matching the spec's "a simple RewardRule" framing.
- **System-triggered audit entries have a null actor** — e.g. the reward-earn transaction fired automatically when an order completes has no human actor, so `AuditLog.actorId` is `null` for that one entry type; everything else is attributed to whoever performed the action.

## Final status (original 12-phase MVP)

All 12 build phases are complete. `npm run dev` / `docker-compose up` both run cleanly, every nav link across all four roles resolves without error, and every rule in Section 7 has been driven through the real app (not just unit-tested in isolation) and confirmed to actually block what it should.

---

## Post-MVP batch — customer-side functionality expansion

A follow-up batch of customer-facing functionality, requested after the 12-phase MVP shipped. Schema-first: one migration adds every new model/field, then each domain was wired end-to-end and verified against the running app + seeded DB before moving to the next.

### Inquiry: customer self-service (edit + cancel)
- Added `InquiryStatus.CANCELLED`. Customers can edit (`updateInquiryAction`) or cancel (`cancelInquiryAction`) their own inquiry only while it's still `NEW` — once staff converts it to a quotation (`QUOTED`) it locks. Both actions are audit-logged (`INQUIRY_UPDATED` / `INQUIRY_CANCELLED`).
- Verified: edited an inquiry's product/qty/description and confirmed the DB updated; cancelled it and confirmed the edit/cancel UI disappears once `status != NEW`.

### Quotation: revision cycle, staff edit/cancel, rush bypass
- Added `QuotationStatus.REVISION_REQUESTED` / `CANCELLED`, a `QuotationRevisionRequest` log model, and `cancelledById`/`cancelReason`/`approvedByStaffId`/`approvalBypassReason` fields on `Quotation`.
- **Customer "Request Changes"** (`requestQuotationRevisionAction`, SENT only): logs the request message, sets the quotation to `REVISION_REQUESTED`, and **converts it back into an Inquiry** — reopening the originally-linked inquiry to `NEW`, or creating one on the fly (product types pulled from the quotation's line items) if the quotation wasn't inquiry-sourced. Staff then re-quotes through the existing Inquiry → Quotation pipeline; `/quotations/new` detects a superseded quotation on the inquiry and prefills its line items plus shows the customer's request message.
- **Staff "Edit Quotation"** (`editQuotationAction`): DRAFT/SENT/REVISION_REQUESTED only — replaces line items and recalculates the total. Locked once `APPROVED`.
- **Staff "Cancel Quotation"** (`cancelQuotationAction`): requires a reason, available any time before approval — covers both "customer asked to cancel" and "pricing mistake" per the request.
- **Staff "Approve for Rush"** (`forceApproveQuotationAction`): bypasses waiting on the customer's own click for rush jobs. Requires a reason, stores who approved it and why, and is audit-logged as `QUOTATION_FORCE_APPROVED` — deliberately distinct from a genuine customer `QUOTATION_APPROVED` so the audit trail never conflates the two. Tightened `approveQuotationAction`/`rejectQuotationAction` to customer-only now that this exists as the staff path.
- Verified end-to-end: customer requested changes on a SENT quote → quotation flipped to `REVISION_REQUESTED` and a new linked Inquiry appeared at `NEW` with the right product type inferred from line items → staff edited the line items (qty change reflected in the recalculated total) → sent it → force-approved it with a reason → audit log showed all four actions in order (`QUOTATION_REVISION_REQUESTED`, `QUOTATION_EDITED`, `QUOTATION_SENT`, `QUOTATION_FORCE_APPROVED`). Separately verified a plain staff cancel with reason.

### Orders/Payments: more methods, proof only where it's needed, vouchers as a payment method
- Added `PaymentMethod.MAYA` and `PaymentMethod.VOUCHER`.
- Customer's payment-proof upload now offers a method choice (GCash / Maya / Bank Transfer / Other) — proof is only ever required for these; Cash stays a staff-only direct-record method (`recordPaymentAction`), and Voucher redemption is self-verifying so it skips proof entirely (see below).
- **Apply Voucher** (`applyVoucherAction`, new): customer picks one of their `AVAILABLE` vouchers from a dropdown filtered to ones whose minimum-spend is met by the order; applying it auto-`CONFIRM`s a `Payment` (method `VOUCHER`) for `min(voucher.value, balance due)` and flips the voucher to `USED`. *Assumption not explicitly confirmed by the business owner and worth double-checking*: minimum-spend is checked against the order's **total** amount, and a voucher can't push the order into credit — it's capped at what's still owed, no "change" credited back.
- Verified end-to-end: applied a ₱200 voucher to a ₱1,200 order — payment recorded, voucher flipped to `USED`, a second unrelated voucher on the same account stayed `AVAILABLE`.

### Rewards: admin-configurable redemption tiers + real vouchers
- New `RedemptionTier` model (`pointsCost`, `voucherValue`, `minimumSpend`, `active`) with full admin CRUD at `/admin/rewards` — this replaces what would otherwise have been a hardcoded denomination table, per explicit request. Unlike the single-active `RewardRule` (earn rate), **multiple redemption tiers can be active at once** (the customer picks one from a dropdown), which is the key behavioral difference between the two config sections on that page.
- New `Voucher` model — customer redemption now goes through `redeemPointsAction` → picks a tier → burns `tier.pointsCost` points → mints a `Voucher` (unique code via `nextVoucherCode()`) carrying that tier's value and minimum-spend, snapshotted at redemption time so later tier-config edits never retroactively change an already-issued voucher.
- Seeded rate: 1 point per ₱500 spent (updated from the MVP's placeholder 1/₱100), 1 point = ₱1, with the four requested tiers (100/200/500/1,000 points → matching-value vouchers, minimum spends ₱500/₱1,000/₱5,000/₱10,000).
- Verified: redeemed a 200-point tier as a customer, got a fresh unique-coded `AVAILABLE` voucher and a correctly decremented balance; confirmed the "Redeem" button is correctly disabled when balance is below every tier's cost (caught this from the seed data itself — a good sign the affordability gate works, not a bug).

### Messaging + notifications (built now, not deferred)
- New `Message` model — a lightweight per-order thread (not full internal email — simpler, no mail infra, and keeps context tied to the order it's about) that both the customer and staff/admin can post to from the order detail page. Posting notifies the other side.
- New `Notification` model + `lib/notifications.ts` (replaces the old `lib/notify.ts` stub) — every notification-worthy event (new inquiry, quotation sent/approved/rejected/revision-requested/cancelled, payment proof uploaded/confirmed/rejected, design draft ready, fulfillment scheduled/delivered/installed/received, new message) now persists a real per-user `Notification` row in addition to the existing `[STUB NOTIFY]` console-log stand-in for SMS/email.
- A notification bell in the Shell header (visible on every page, all roles) shows an unread-count badge, a dropdown of recent notifications, click-to-navigate-and-mark-read, and "mark all read".
- Verified end-to-end: the seeded unread message notification showed the correct badge count for the customer, opening the dropdown and clicking it navigated to the order and flipped it to read in the DB; sending a fresh message created a new unread notification for the other party.

### Seed data additions
Added to demonstrate every new flow on first run without any manual setup: a `REVISION_REQUESTED` quotation with its reopened inquiry and logged request message, a staff-`CANCELLED` quotation with reason, four `RedemptionTier` rows, an `AVAILABLE` voucher plus enough bonus points for a customer to redeem a second one live, a sample two-message thread on an existing order, and a few unread notifications so the bell isn't empty on first login.

### Also fixed while in here
`.gitignore`'s blanket `.env*` pattern was silently swallowing `.env.example` too, so it had never actually been committed despite the README instructing `cp .env.example .env` — added a `!.env.example` exception and committed the file for real.

## Known Stubs (as of the Aug 14 batch)

- **SMS/Email notifications** — `lib/notifications.ts` persists a real `Notification` row per recipient (backing the in-app bell) but still just `console.log`s a `[STUB NOTIFY]` line for the actual SMS/email send. A real build would wire that half to Twilio/SendGrid.
- **Payment gateway** — payments are manually recorded by staff, uploaded as proof by the customer, or paid via voucher (internal ledger only); nothing talks to an actual payment processor or e-wallet API.
- **Password reset / email verification** — not implemented.
- **Courier tracking** — tracking number/courier are free-text fields, no live courier API integration.
- **Invoice/PDF generation** — not built (spec marks this optional).
- **Object storage** — files are written to local disk (`public/uploads/`) with metadata in the `File` table; a production deployment would swap this for S3-compatible storage behind the same `lib/upload.ts` interface.
- **Single active reward *earn* rule** — only one `RewardRule` (earn rate) is active at a time; redemption tiers, by contrast, support multiple simultaneously active tiers by design.
- **System-triggered audit entries have a null actor** — e.g. the reward-earn transaction fired automatically when an order completes has no human actor, so `AuditLog.actorId` is `null` for that one entry type.
- **Messaging is per-order only** — no pre-order (inquiry-level) messaging thread yet; could be extended the same way if needed. *(Superseded by the Aug 15 chatbox rework below — Message is no longer Order-only.)*
- **Voucher minimum-spend / cap assumption** — see the Orders/Payments section above; this was a default choice, not an explicitly confirmed business rule.

---

## August 15 system update

A follow-up batch addressing a formal "AUGUST 15 SYSTEM UPDATE" requirements document: quotation control, the still-pending Aug 14 notification triggers, a full chatbox (not just Order-scoped), a centralized customer Payment tab, a faster staff payment-recording flow, and a real mobile-responsive pass. Every item below was verified against the running app (Playwright-driven, cross-checked against the DB) before moving to the next, and everything from the Aug 14 batch (inquiry edit/cancel, quotation revision cycle, reward tiers, multi-method payments, notification bell) was re-verified working, not just left alone.

### Quotation control
- `Quotation.createdById` (+ `createdBy` relation) records who prepared each quotation; the detail page now shows "Prepared by `<name>` on `<date>`".
- Duplicate-quotation prevention: `lib/quotation-status.ts` defines the "active" statuses (`DRAFT`/`SENT`/`REVISION_REQUESTED`/`APPROVED`); `createQuotationAction` now blocks creating a second active quotation on an inquiry that already has one, and the inquiry detail page shows staff a direct link to the existing quotation to revise instead of letting them start a competing one.

### Chatbox (Inquiry / Quotation / Order / Job Order / General)
- Replaced the Order-only `Message` model with a subject-agnostic `Conversation` (`INQUIRY`/`QUOTATION`/`ORDER`/`JOB_ORDER`/`GENERAL`) + per-user `ConversationRead` for unread tracking; migration backfills every existing Order-scoped message into a Conversation row before dropping the old FK (verified against both a populated and an empty DB).
- `lib/conversations.ts` (get-or-create, mark-read, subject label/source-link helpers) and a rewritten `sendMessageAction` that posts against a `conversationId`.
- `ConversationCard` (last message preview + unread badge + Open/Start Chat) is embedded on Inquiry, Quotation, and Job Order detail pages; the Order page keeps a full inline thread. New `/messages` inbox (unread badges; staff see every customer's conversations) and `/messages/[id]` full-thread routes, plus a "New General Message" entry point for customers not tied to any specific record. "Messages" added to the nav for every role that can use it.
- Verified: customer send → staff notification with a working link → staff reply, exercised on all four subject types (inquiry/quotation/order/job order) plus a general conversation.

### Notifications — remaining Aug 14 triggers
Clicking a notification already redirected to its `link` (`openNotificationAction`), so "actionable" was already satisfied — the actual gap was missing trigger events. Added, all routed through the existing `notifyCustomer`/`notifyStaff` helpers with a link:
- Order created, order completed.
- Production/stage-progress updates (stage advance, entering QC, QC pass/fail) from `lib/workflow.ts`.
- Job order completion (distinct from the fulfillment delivered/received/installed notices).
- Delivery "in transit" (previously only "delivered" notified).
- Reward points earned, voucher redeemed, voucher used.
- Outstanding balance reminder: a staff/admin "Send Balance Reminder" button on the order page — there's no background job runner in this prototype to fire these automatically, so it's operator-triggered rather than scheduled.

### Customer Payment tab + inline Record Payment
- `/payments` is now open to `CUSTOMER` (role-branched content, not a separate route): customers get a centralized view across every order — total amount paid, outstanding balance, count of orders with a balance due, a per-order balance table, and full payment history (date, amount, method, order, reference #, status, proof) — without opening each order individually. Staff/admin keep the original all-payments view.
- `RecordPaymentDialog`: a modal opened directly from the Order page, pre-populated with the order number, customer name, and outstanding balance; staff only fill in amount, method, reference number, payment date, and notes — no more bouncing to `/payments` and re-selecting the same order. `Payment.referenceNumber`/`Payment.paymentDate` (schema fields already present) are now actually collected. The `/payments` fallback form gained the same fields plus order pre-selection via `?orderId=`.

### Mobile responsiveness
The prior layout had no mobile nav at all (sidebar was `hidden md:flex` with nothing replacing it below that breakpoint) — fixed first, then a pass over fixed-column grids and cramped multi-field rows app-wide:
- `MobileNav`: hamburger-triggered slide-out drawer reusing the same `SidebarNav`.
- Header/main padding and the notification bell dropdown adapt to narrow viewports.
- Every fixed multi-column grid (`grid-cols-2/3/4/5/12`) across dashboards, order/job-order/quotation detail pages, and admin forms now starts single-column and only expands at `sm`/`md`/`lg`, instead of squeezing fields into slivers.
- `justify-between` title+action header rows wrap instead of forcing a title and button onto one line.
- The quotation line-items editor (5 fields + remove, packed into a 12-col grid) got an actual mobile layout — product/description full-width, qty/price side by side, remove right-aligned — rather than a shrunken desktop row.
- Tables already scrolled horizontally inside their own container (`components/ui/table.tsx`), so no change was needed there.
- Verified with Playwright at a 375px viewport: zero horizontal page overflow across every staff/admin and customer page, the mobile nav drawer, and the notification bell.

### Known stubs, still true after this batch
Everything in the "Known Stubs (as of the Aug 14 batch)" list above still applies — this batch didn't touch payment-gateway integration, SMS/email delivery, courier APIs, or object storage. The one item explicitly resolved is per-order-only messaging (see chatbox section above).

---

## August 15 — 2nd update: User Roles & Staff Permissions

A formal "USER ROLES & PERMISSIONS" requirements document asked for a proper Admin-controlled, per-Staff-member permission system — not three fixed roles — with explicit emphasis that enforcement must be real at the backend/API level, not just hidden buttons. Started by reading the existing auth stack (NextAuth v5 credentials + JWT in `lib/auth.ts`/`lib/auth.config.ts`, `requireUser()`/`requireRole()` in `lib/session.ts`, route gating in `proxy.ts`) before adding anything, per the request's explicit instruction to reuse rather than parallel it.

### Design
- **ADMIN**: unchanged — `requireRole`/`requirePermission` both always let ADMIN through unconditionally. Never touches the permission table.
- **CUSTOMER**: unchanged — never participates in the permission system, isolated exactly as before.
- **PRODUCTION**: unchanged — keeps the same unrestricted access to production/QC actions it always had (passed as a `bypassRoles` escape hatch to `requirePermission`), since this update was about STAFF, not about folding PRODUCTION into it.
- **STAFF**: access now comes entirely from an admin-assigned set of granular permissions instead of the role itself. A new `Permission` enum (50 values across Inquiry/Quotation/Orders/Payments/Production/Fulfillment/Rewards/Communication/Reports/Customer Management/User Management, matching the spec's categories) plus a `StaffPermission` join table (existence of a row = granted) replace the old blanket `requireRole(["STAFF","ADMIN"])` checks.

### Schema + migration
- `Permission` enum, `StaffPermission` model (`@@unique([userId, permission])`), `User.active Boolean @default(true)` for activate/deactivate.
- The migration is purely additive (new enum/table, `ADD COLUMN ... DEFAULT true`) but also **backfills every existing STAFF user with every permission** in the same migration, so introducing this system doesn't silently strip access from anyone who could already do everything a STAFF role could do — Admin dials individual staff back afterward. Verified this specifically: seeded two pre-existing STAFF users into a copy of the DB *before* applying the migration, then applied it, and confirmed both landed with all 50 permissions while a pre-existing ADMIN got zero rows (bypasses the table entirely). New STAFF accounts created after this migration start with zero permissions until Admin assigns some.

### Enforcement
- `lib/permissions.ts` — data/types only (Permission list, category labels, `PERMISSION_PRESETS`), deliberately kept free of any Prisma import so it's safe to import from Client Components (the permission checkbox grid needs it).
- `lib/permissions-guard.ts` — the DB-backed half: `getStaffPermissions()` (React-`cache()`-deduped per request), `can(user, permission)`, and `requirePermission(permission, bypassRoles?)` — the server-action guard that replaced `requireRole` at every relevant call site across `inquiries.ts`, `quotations.ts`, `orders.ts`, `payments.ts`, `fulfillment.ts`, `production.ts`, `qc.ts`, `rewards.ts` (reward *config* actions only — redemption stays customer-only), and `messages.ts` (staff-sender path). `inventory.ts` and `workflow-templates.ts` were deliberately left on the old `requireRole` — those categories aren't in the spec's permission list, so they're out of scope; `admin-users.ts`'s user-creation/permission-assignment actions stay `requireRole(["ADMIN"])` on purpose, since letting a STAFF permission grant control access to the page that assigns STAFF permissions would be a privilege-escalation hole.
- Frontend visibility now mirrors the backend: every button/form gated by a specific permission checks `can(user, "X")` before rendering (view-level page guards redirect to `/dashboard` for STAFF lacking the page's `*_VIEW` permission), and the sidebar/mobile nav (`nav-config.ts`) filters STAFF items by the same granted-permission set via `Shell` fetching `getStaffPermissions()` once per request.
- `proxy.ts`: opened `/production` and `/admin/rewards` to STAFF at the route level (permission-gated inside), since a STAFF account can now be granted production or reward-config permissions; every other `/admin/*` path stays ADMIN-only.
- Deactivation (`User.active`): checked in `authorize()` (blocks login outright) and again in `requireUser()` on every request (DB re-check, since JWT sessions don't self-invalidate) — a deactivated user is locked out immediately, not just on next login.

### Admin UI
- `/admin/staff-permissions`: lists STAFF accounts with active status and a permission count.
- `/admin/staff-permissions/[userId]`: a checkbox grid grouped by category (client component, presets from `lib/permissions.ts`), a preset dropdown that pre-checks boxes without locking them (still individually editable before saving), backed by `updateStaffPermissionsAction` (replace-the-full-set, `requireRole(["ADMIN"])`).
- `/admin/users`: added an Active/Deactivated badge and an activate/deactivate toggle (`toggleUserActiveAction`, blocks self-deactivation).

### Verification
Reseeded from scratch through all 4 migrations (zero drift via `prisma migrate diff --exit-code`), then Playwright-verified against the running app:
- ADMIN reaches every route including `/admin/staff-permissions`, `/production`, `/admin/rewards` unconditionally.
- A "Manager"-preset STAFF (`staff1`, 45/50 permissions, seeded via `PERMISSION_PRESETS.Manager`) sees the full nav and every button; a "Sales Staff"-preset STAFF (`staff2`, 10/50 permissions) has `/payments` and `/production` redirect them to `/dashboard`, and does not see Record Payment / Send Balance Reminder / Add Job Order on an order it *can* view.
- **Backend enforcement, concretely, not just UI hiding**: captured the exact `Next-Action` POST request staff1 (has `PAYMENT_RECORD`) sends when submitting the Record Payment dialog, then replayed the byte-identical request with only staff2's session cookie swapped in. The server rejected it — HTTP 500, `"You do not have permission to do this (Record payment)."` thrown from `requirePermission` inside `recordPaymentAction` — and confirmed via direct DB query that zero rows were created by the replay (only staff1's original, legitimate click persisted). This proves the check runs on the server regardless of what the client sends, not just when a button happens to be visible.
- Deactivating staff2 from `/admin/users` immediately blocked their login attempt with "This account has been deactivated."; reactivating restored it.
- Full route crawl across ADMIN/STAFF(Manager)/STAFF(SalesStaff)/PRODUCTION/CUSTOMER: zero 500s, zero page errors.
- Re-verified every "must not break" item from the spec's own checklist still works: inquiry edit/cancel, quotation prepared-by + duplicate-quotation prevention, customer Payment tab, rewards page, notification bell. The new permission-grid page itself was also checked at a 375px mobile viewport — categories stack cleanly, no horizontal overflow.

### Known gaps / deliberate scope decisions
- Several permissions in the enum have no enforcement point yet because the underlying feature doesn't exist in the app: `INQUIRY_MODIFY`/`INQUIRY_CANCEL` (no staff-side inquiry edit/cancel action exists — only the customer's own), `ORDER_HANDLE_MODIFICATION`/`ORDER_UPDATE_STATUS`/`ORDER_CANCEL` (order status changes are all system-driven; there's no manual "cancel an order" action), `PAYMENT_EDIT`/`PAYMENT_REFUND` (no edit/refund action exists), `REPORTS_VIEW`/`REPORTS_EXPORT` (no reports page exists), `CUSTOMER_*` (no staff-facing customer CRUD exists — customers only self-register). They're defined and selectable in the permission grid/presets for schema completeness and so Admin can pre-configure them ahead of those features landing, but granting them today has no effect.
- `USER_VIEW`/`USER_CREATE`/`USER_EDIT`/`USER_ACTIVATE_DEACTIVATE`/`USER_MANAGE_PERMISSIONS` are likewise defined but not wired to a STAFF bypass — those pages are ADMIN-only at the route level by design (see privilege-escalation note above), so granting them to a STAFF account currently does nothing.
- Inventory and Workflow Templates remain on the old role-based (`requireRole`) gating — untouched because the spec's permission categories don't mention them.

---

## August 15 — 3rd update: Customer Payment nav tab + real-time chat

Two-part request: add a "Payment" item to the customer sidebar (the page already existed from the earlier Payment tab work, it just wasn't linked from the nav), and upgrade chat from refresh-based to real-time — explicitly instructed to inspect and upgrade the existing `Conversation`/`Message` architecture rather than build a parallel system.

### Customer sidebar
`nav-config.ts`'s `CUSTOMER` branch gained `{ label: "Payment", href: "/payments" }` between "My Orders" and "My Rewards", matching the requested order exactly (verified in the mobile drawer too, since it reuses the same `navForRole()` output). No new page — `/payments` already branches to the consolidated customer view (history, total paid, outstanding balance, remaining due per order) built in the first Aug 15 update.

### Real-time chat — architecture
This stack is plain PostgreSQL via Prisma (not Supabase), so there's no built-in logical-replication realtime channel to hook into. Rather than adding an external pub/sub service (Pusher/Ably/etc.) or standing up a separate WebSocket server, used what a persistent Node.js server (Render, not serverless) already supports natively: **Server-Sent Events** backed by an **in-process pub/sub**.

- `lib/realtime.ts`: a per-user pub/sub (`subscribeUser`/`publishToUser`/`publishToUsers`) on top of a Node `EventEmitter`. Hit a real bug here worth recording: a plain module-scope `new EventEmitter()` produced *two independent instances* in practice — Route Handlers and Server Actions compile into separate module graphs (visible even within one Node process, especially under Turbopack dev) — so the SSE route's subscription and the Server Action's publish never saw each other. Diagnosed by tagging each instance with a random ID and logging both sides; fixed the same way `lib/prisma.ts` already avoids the equivalent HMR-duplication problem for `PrismaClient` — stash the EventEmitter on `globalThis` so every module graph in the process shares the one instance. Documented as a single-server-instance design (correct for this deployment; horizontal scaling would need a real shared bus like Postgres `LISTEN/NOTIFY` or Redis).
- `app/api/realtime/route.ts`: an authenticated (session + `User.active` checked) SSE Route Handler that subscribes the current user and streams `data: {...}\n\n` events with a 20s heartbeat, cleaning up on `req.signal` abort.
- Publish points, reusing the existing fan-out logic rather than duplicating it: `lib/notifications.ts`'s `notifyUser()` now publishes a `notification` event after creating the row — this alone makes the bell live for *every* existing notification trigger app-wide (order updates, QC, fulfillment, rewards, not just chat), for free. `sendMessageAction` publishes a `message` event to the same recipient set `notifyCustomer`/`notifyStaff` already notifies, plus the sender's own userId (so the sender's bubble appears through the same round-trip rather than a separate optimistic-UI code path — one source of truth, no dedup bugs). The action's old `redirect()` after sending was removed; the composer now clears itself once the pending state resolves.
- Client side: `RealtimeProvider` (mounted once in `Shell`) opens the single `EventSource` and rebroadcasts via `window` `CustomEvent`s (`realtime:message` / `realtime:notification`) rather than React Context, so independently-mounted client islands (bell, an open thread, the inbox list) can each listen without connection multiplication or prop-drilling. `NotificationBell` holds local state seeded from props and prepends live notifications / bumps the unread count. `MessageThread` appends live messages (dedup by id), auto-scrolls to bottom, and is now the single source of truth for the thread's contents post-mount. A small `RefreshOnMessage` component calls `router.refresh()` on any relevant event — dropped onto the `/messages` inbox list and the `ConversationCard` preview on Inquiry/Quotation/Job Order pages so "latest message preview" and unread badges update live there too, without turning those Server Components into client components.

### Verified end-to-end (two concurrent, independently-authenticated Playwright browser contexts)
- Customer sends → appears on staff's already-open thread with **zero page navigation** (tracked via `framenavigated` listeners on both pages) and zero manual refresh; staff replies → same in reverse.
- A user's own sent message appears through the same SSE round-trip as the recipient's, confirmed independently.
- Notification bell badge count increases live while staff sits on an unrelated page (`/dashboard`, not the conversation) — proves the notification path works independently of the message-thread path.
- `/messages` inbox list's last-message preview updates live while staff sits on the list itself, not inside a thread.
- Reload/reconnect: history persists correctly (it's just a normal DB read on mount — never depended on the SSE connection).
- Full route crawl re-run across ADMIN/STAFF(broad)/STAFF(narrow)/PRODUCTION/CUSTOMER after these changes: zero errors, confirming the permission system from the 2nd update wasn't disturbed.
- Mobile: sidebar drawer shows the exact requested order (Dashboard → My Inquiries → My Quotations → My Orders → Payment → My Rewards → Messages) at 375px with no overflow; the chat thread renders correctly on the same viewport (bubbles, timestamps, composer) with the message history from the live test visible after reload.

### Known limitation
Single-server-instance pub/sub (see `lib/realtime.ts`'s own doc comment) — correct for the current deployment, would need a shared bus if ever scaled to multiple Render instances behind a load balancer.

---

## August 15 — 4th update: Floating Messenger-style chat widget

Replaces the "go to a dedicated Messages page" flow with a Facebook-Messenger-style floating widget for the CUSTOMER portal, built entirely on the existing Conversation/Message/real-time infrastructure — no parallel chat system, and the full `/messages` pages stay exactly as they were (still linked from the widget's list view as "View full message history").

### What was added
- `FloatingChatWidget` (`components/messaging/floating-chat-widget.tsx`), mounted once in `Shell` only when `role === "CUSTOMER"` — visible on every Customer Portal page (Dashboard, My Inquiries, My Quotations, My Orders, Payment, My Rewards) without a route change.
- A circular button fixed bottom-right with an unread badge (💬), fetched on mount so it's accurate even before the customer ever opens it. Clicking opens the panel; clicking again minimizes it (same button, same handler — matches "click again to minimize").
- Panel has two views sharing one component: a compact **conversation list** (subject label, last-message preview, per-conversation unread badge, "New General Message", a link to the full `/messages` page) and a **thread view** that embeds the *existing* `MessageThread` component unmodified in behavior — same bubbles, same real-time listener, same composer. Opening the widget for the first time in a session jumps straight into the most recently active conversation (matching how Messenger itself behaves when you have something to catch up on); a back arrow in the header returns to the list to switch threads.
- New read-only data actions in `app/actions/messages.ts` (`getMyConversationsAction`, `getConversationMessagesAction`, `markConversationReadAction`, `openOrCreateGeneralConversationAction`) — same queries/shapes the `/messages` pages already used, just exposed as plain callable Server Actions so the globally-mounted client widget can fetch on demand instead of relying on a server-rendered page load. No new tables, no new fan-out logic.
- Real-time behavior is inherited, not rebuilt: the widget listens to the same `realtime:message` window event `RealtimeProvider` already broadcasts. On every event it re-fetches the conversation list (keeping the badge and previews authoritative) and, if the event is for the conversation currently open in the widget, marks it read server-side so it doesn't stay "unread" while actively being looked at.
- Mobile: the button stays fixed bottom-right at every viewport size; the open panel is `fixed inset-0` (near-fullscreen) below the `sm` breakpoint and a bounded `24rem`-wide, `32rem`-tall floating box above it — matching "expanding to most or all of the screen" on mobile vs. a proper floating widget on desktop/tablet.
- Polish fix: `MessageThread` gained an opt-in `fillHeight` prop (default off) that swaps its fixed `max-h-96` message area for a `flex-1` one so it stretches to fill the widget's bounded panel instead of leaving dead space below the composer on the mobile fullscreen layout. Off by default, so the Order page's inline thread and the full `/messages/[id]` page — which don't have a definite parent height — render exactly as before; verified via screenshot that neither changed.

### Verified
- Widget button present on every customer page tested (Dashboard, Inquiries, Quotations, Orders, Payments, Rewards); confirmed absent for STAFF.
- Open → auto-jumps into the most recent thread → minimize (click again) hides it → reopen restores the same thread (not back to the list).
- Two-session real-time test: staff replies via the full `/messages/[id]` page → appears in the customer's already-open widget instantly; customer sends from inside the widget → appears on staff's page instantly and shows correctly attributed on the customer's own side.
- Minimized-widget unread badge increments live when a new message arrives while the widget is closed, with the correct count.
- Mobile (375px): zero horizontal overflow in the closed, open-thread, and list states; screenshots confirm the thread now fills the available height with the composer pinned to the bottom instead of a dead gap.
- Full route crawl re-run across every role/permission combination from the earlier updates: zero regressions.

---

## August 15 — 5th update: Professional UI/UX + red/white branding + centralized business settings

A UI/UX enhancement pass, explicitly scoped to not touch any working business logic from the earlier August 15 updates. Rather than reskinning every page individually, the change went through the app's existing shared component layer so the rebrand cascades automatically and stays a "reusable design system" rather than a one-off coat of paint.

### Design system foundation
- `app/globals.css`: added a `brand-50`…`brand-900` red scale as CSS custom properties (Tailwind v4's CSS-based `@theme inline` config, no `tailwind.config.js`), wired `--font-sans` to Montserrat.
- `app/layout.tsx`: swapped Geist Sans for `next/font/google`'s Montserrat (weights 400–800); `generateMetadata()` now reads the page `<title>`, description, and favicon from `getBusinessSettings()` instead of static strings.
- Shared primitives updated once, inherited everywhere: `components/ui/button.tsx` (default variant → `bg-brand-600`), `components/ui/input.tsx` (focus ring → `brand-500` on Input/Textarea/Select), `components/layout/sidebar-nav.tsx` (active item → `border-l-2 border-brand-600 bg-brand-50 text-brand-700`), plus the messaging components (chat widget header/button, "mine" bubble color, notification-bell unread row) switched from slate/blue to the brand color. `Card`, `Badge`/`StatusBadge`, and `Table` were deliberately left neutral — the spec explicitly warned against making the whole interface red, and status-color semantics (green/red/yellow badges for PASS/FAIL/PENDING etc.) needed to stay independent of the brand accent.

### Centralized Business Settings (new: no more hard-coded business identity)
- New `BusinessSettings` Prisma model — a singleton row (`id @default("default")`, upsert-only writes) holding Business Identity (name, tagline, description, logo path, favicon path), Contact Information (contact number, email, Facebook URL, website), and Business Address (address line, city, province, postal code). Migration `20260817090000_business_settings` creates the table and seeds the singleton row with sensible defaults so the app never has to special-case a missing row.
- `lib/business-settings.ts`: `getBusinessSettings()` (React `cache()`-deduped per request, with an in-code fallback if the row were ever absent) is now the single source of truth every branded surface reads from — change it once in the admin UI, it propagates everywhere without touching another file.
- `app/(app)/admin/settings/` (new): an Admin-only page (`requireRole(["ADMIN"])`) with a sectioned form (Business Identity / Contact Information / Business Address) backed by `updateBusinessSettingsAction` — validates via zod, reuses the existing `saveUploadedFile()` upload helper for logo/favicon (no new upload infrastructure), only overwrites `logoPath`/`faviconPath` when a new file is actually chosen, and audit-logs the change. Linked from the Admin nav as "Business Settings".
- Wired into every branded surface: root `<title>`/description/favicon (`app/layout.tsx`), the login/register split-screen (`app/(auth)/layout.tsx` — logo or a letter-avatar fallback, business name, tagline, description), and the app `Shell` (`components/layout/shell.tsx`) — desktop sidebar header, mobile header bar, and the mobile nav drawer (`components/layout/mobile-nav.tsx`) all now render the admin-configured name/tagline/logo instead of the literal string "LP Printing" that used to be scattered across three files.

### Login page redesign
`app/(auth)/layout.tsx` + `app/(auth)/login/*`: two-column desktop layout (left: brand-red panel with logo/name/tagline/description; right: white form panel), collapsing to a single column on mobile. `login-form.tsx` gained a show/hide password toggle (`lucide-react` Eye/EyeOff icon button inside the password field) and a "Welcome back" heading; the register page got the matching heading treatment. Existing behavior (error Alert on bad credentials, demo-accounts hint box, `loginAction` server action) untouched.

### Dashboard polish
- Customer dashboard (`app/(app)/dashboard/page.tsx`): added a subtext line under the welcome heading; `StatCard` restyled with an uppercase tracking-wide label, a larger bold value (`text-2xl md:text-3xl`), and a `border-l-4` accent — `border-l-brand-600` normally, `border-l-amber-400` when a card needs attention (quotations awaiting approval, balance due > 0). The generic STAFF/PRODUCTION fallback dashboard got the same accent-border treatment on its quick-link cards.
- Admin dashboard (`app/(app)/admin/dashboard/page.tsx`): added two new stat queries the spec explicitly asked for — **New Inquiries** (`Inquiry` count where `status: "NEW"`) and **Pending Payments** (`Payment` count where `status: "PENDING"`) — both now lead the stat grid ahead of Open Quotations, followed by Outstanding Balance, QC Pass Rate, Low-Stock Items, and the existing monthly customer/rewards figures. This file's own `StatCard` helper (a separate component from the customer dashboard's) was restyled to match — same uppercase-label/larger-value/`border-l-4` treatment, `tone` renamed from `"yellow"` to `"attention"` for consistency between the two dashboards.

### Verification
- `npm run build` passes clean (Turbopack production build, all 26 routes compile, zero type errors) after every step, including the final dashboard changes.
- `npx prisma migrate status` / `migrate diff --exit-code`: zero drift, `BusinessSettings` migration applied cleanly on top of the existing 4 migrations.
- Full Playwright regression pass across all 4 roles (ADMIN, STAFF, PRODUCTION, CUSTOMER) plus mobile-viewport screenshots of the login page, both dashboards, the floating chat widget, and the new Business Settings page — re-confirming every item on the spec's own "must not break" list: inquiries, quotations (incl. duplicate-quotation prevention and prepared-by info), orders, payments, the customer Payment nav tab, payment recording, rewards, notifications and their redirects, real-time chat, the floating Messenger-style widget, staff permissions, production, fulfillment, and mobile responsiveness.

### Known gaps / deliberately deferred
- No dashboard charts were built — the spec said "charts where useful" as a suggestion, not a requirement, and the stat-card + list-section layout already answers the "what needs my attention" question the spec asked for; adding a charting dependency was judged higher risk/lower value than the rest of this update given the scope already covered.
- Business name/logo are not yet threaded into quotation/order/payment page *content* (only into the app chrome: login, sidebar, header, browser tab) — there's no PDF/document-generation feature in the app yet to attach a letterhead to, so there was nothing concrete to wire that data into beyond what's already centralized in `getBusinessSettings()` for whenever that feature exists.
- Full route crawl re-run across every role/permission combination from the earlier updates: zero regressions.

---

## August 15 — 6th & final update: dashboard charts, transaction branding, Admin/Staff chat

Closes the two documented gaps from the 5th update and extends the Customer floating chat to Staff and Admin on the same underlying infrastructure — one messaging system, role-gated by the existing permission model, not a parallel implementation per portal.

### 1. Dashboard charts (Recharts, already a dependency)
`components/dashboard/admin-charts.tsx` (new, client component) adds three charts to `/admin/dashboard`, replacing the old plain "Job Orders by Stage" list and adding two new cards:
- **Orders by Status** — bar chart from a new `Order.groupBy(["status"])` query.
- **Revenue & Orders Trend** — a combo chart (bar = order count, line = confirmed payment revenue) over the last 6 calendar months, covering both "Revenue/Payment Trend" and "Orders/Transactions over time" in one useful view instead of two overlapping ones.
- **Production Status Overview** — the former plain list of job-order counts by stage, now a horizontal bar chart (same underlying `jobOrder.groupBy` query, no new data need).
Bar/segment colors reuse the exact hex values `components/ui/badge.tsx`'s `STATUS_TONE` mapping already uses per status, so a chart's colors match the status badges seen everywhere else in the app. Customer dashboard was deliberately left without a new chart — the stat cards and order/quotation lists already answer "what do I need to do," and the spec explicitly warned against adding a chart just to check a box.

### 2. Business branding on transaction pages
`components/branding/transaction-brand-header.tsx` (new, server component) renders a compact single-row strip — logo-or-letter-avatar, business name, and contact number/email if configured — sourced from the existing `getBusinessSettings()`. Added to the top of `/quotations`, `/quotations/[id]`, `/orders`, `/orders/[id]`, and `/payments` (both the Customer and Staff/Admin views of that last page). No hardcoded business identity anywhere in these files; an Admin changing the name/logo/contact info in Business Settings updates all five pages automatically. Kept deliberately small (one row, no page-content changes below it) per the "don't take up excessive space" instruction.

### 3. Floating Messenger-style chat extended to Staff and Admin
The exact same `FloatingChatWidget` component, `Conversation`/`Message` tables, and SSE real-time pipe built for Customers is now mounted for Staff (gated) and Admin too — inspected the existing implementation first and extended it rather than building a second chat surface, per the update's own instruction.

- **Mounting** (`components/layout/shell.tsx`): the widget now renders for `CUSTOMER` (always), `ADMIN` (always — bypasses permissions like every other admin check in this app), and `STAFF` only if their account holds `COMMUNICATION_VIEW` — reusing the `staffPermissions` Set the Shell already fetches for nav filtering, so no extra query.
- **Data actions** (`app/actions/messages.ts`): `getMyConversationsAction` now branches on role — Customers get their own conversations as before, Staff/Admin get every customer conversation (matching the access model the full `/messages` staff view already used), each with the customer's name and a computed reference label (see below). `getConversationMessagesAction` now also enforces `COMMUNICATION_VIEW` for Staff (previously it only checked *role*, not the specific permission — a gap this update closed) and returns a `canSend` flag alongside the messages, computed from `COMMUNICATION_SEND`. `markConversationReadAction` picked up the same `COMMUNICATION_VIEW` check. `sendMessageAction` already enforced `COMMUNICATION_SEND` for Staff/Admin from the 3rd update — unchanged.
- **View-only enforcement, not just hiding**: `MessageThread` gained an optional `canSend` prop (default `true`, so every existing call site is unaffected); when `false` it replaces the composer with a plain "You have view-only access to this conversation" note instead of a form that would only fail after submit. The gate is real on the server too — `sendMessageAction`'s existing `COMMUNICATION_SEND` check throws regardless of what the client renders, so a Staff account with view-only access can't send by replaying a request either.
- **Transaction context** (`lib/conversations.ts`): new `conversationReferenceLabel()` builds a label with the actual record number where available — `"Quotation #QT-2026-0001"`, `"Order #ORD-2026-0004"`, `"Job Order #JO-002"`, `"Inquiry: <product>"`, or `"General Support"` — replacing the old label that only said the generic subject type. Used in the widget for both roles (customers now also see their own quote/order number instead of just "Quotation"), and backported into the full `/messages` list and `/messages/[id]` pages for consistency. The now-unused `conversationSubjectLabel()` was deleted rather than left dead.
- **Staff/Admin-specific UX**: conversation rows show the customer's name as the primary line and "Re: {reference}" as a secondary line (Customers just see their own reference label, they don't need their own name). Staff/Admin land on the conversation list when opening the widget rather than auto-jumping into the latest thread — appropriate for an inbox that can span many unrelated customers, versus a Customer's handful of own conversations where jumping straight to the newest one matches how Messenger itself behaves.
- **Real-time**: no changes needed to the SSE pipe, `RealtimeProvider`, or the publish points in `sendMessageAction` — Staff and Admin already received `message`/`notification` events for every conversation (that's how the full `/messages` page and notification bell already worked), so wiring the widget into the same `window` `CustomEvent` listeners the Customer widget already used was enough to get live badge counts, live list previews, and live thread updates for free.
- **Known limitation, called out rather than silently skipped**: the spec asked for Staff↔Admin direct messaging "if supported by the existing architecture." It isn't — `Conversation.customerId` is a required field, so every conversation is anchored to a customer by design; there's no concept of a staff-to-staff or staff-to-admin thread in the schema. Building that would mean a schema change (a nullable `customerId` plus a participants join table) and a second UI mode, which is a meaningfully different feature rather than an extension of the existing one — left out of this update rather than bolted on as a special case that doesn't fit the data model.

### Verification
- `npx tsc --noEmit` and `npm run build` both pass clean after every step.
- `npx prisma migrate diff --exit-code`: no schema changes were needed — `COMMUNICATION_VIEW`/`COMMUNICATION_SEND`/`COMMUNICATION_MANAGE` already existed in both the `Permission` enum and the database enum from the 2nd update's permission system, so this update only had to start *enforcing* them in two more places (`getConversationMessagesAction`, `markConversationReadAction`) rather than adding anything new to the schema.
- Full Playwright regression pass, all sections passed with zero console errors and zero visual regressions:
  - **Charts**: all three render correctly on desktop and mobile, no horizontal overflow.
  - **Branded headers**: confirmed subtle and present on all five transaction pages for both Customer and Staff views.
  - **Staff/Admin chat**: bubble present for staff1/admin on every reachable page, absent for PRODUCTION; conversation list shows customer name + "Re: {reference}"; two-context real-time test confirmed Customer→Staff and Staff→Customer messages and unread badges update live with zero reloads.
  - **Permission enforcement**: walked a Staff test account through full access → view-only (composer replaced by the notice text) → no access (widget absent) → restored to its original preset, verified via both the admin UI and a direct DB query. A replayed `sendMessageAction` POST with `COMMUNICATION_SEND` stripped returned no new message row, confirming the guard rejects server-side rather than relying on the UI hiding the composer.
  - **Regression smoke pass**: Inquiries, Quotations (quote numbers intact), Orders, Payments, Rewards, notification bell, Business Settings, mobile nav, and login/logout all still work across ADMIN/STAFF/CUSTOMER.
  - Minor non-blocking observation: the dev log showed occasional `destination stream closed early` entries on the `/api/realtime` SSE endpoint under the test's rapid browser-context churn — the `controller.enqueue`/`close` calls in `app/api/realtime/route.ts` are already wrapped in `try/catch`, this is Node-level cleanup noise from abrupt disconnects, not a page-level error, and didn't affect any test outcome.


---

## August 15 — 7th update: the Chatbox becomes the central communication hub

Ownership/assignment, transfers, private and group chat, attachments, and message-level transaction references — all on the existing Conversation/Message/SSE infrastructure, extended rather than replaced.

### Architecture decision
Rather than merging existing per-transaction conversations into one thread (a destructive migration), everything was added on top, additively:
- `Conversation.type` (CUSTOMER / CUSTOMER_GROUP / PRIVATE / GROUP) lets one messaging system represent customer conversations, internal Staff↔Admin DMs, and group chats without a parallel system per kind.
- The customer's "central Chatbox" reuses the existing `GENERAL` subject type (`getOrCreateGeneralConversation`) — no new conversation-creation path needed.
- Old per-transaction conversations (Inquiry/Quotation/Job Order/Order, from before this update) are untouched historical data, still reachable via `/messages`. New Inquiry/Quotation/Job Order references now attach to *messages* inside the central conversation instead of spawning a new conversation per transaction (Order was excluded from this — it was never in the spec's list of removed links or supported reference types, so its own embedded thread on the Order detail page keeps working exactly as it did, just made ownership-aware).
- `Conversation.customerId`/`subjectType` were made nullable (PRIVATE/GROUP have neither) — an additive migration, no data loss.

### Ownership, transfer, reassignment, takeover
- A new CUSTOMER conversation starts unassigned. The rule, enforced in `app/actions/messages.ts`'s `sendMessageAction`: the first Staff/Admin who *sends* a reply (never merely opens/views one) becomes `assignedStaffId`, logged as an inline SYSTEM message ("X is now handling this conversation").
- Once assigned, `computeCanSend()` blocks every other Staff/Admin from sending — including Admin, who is treated the same as Staff here per the spec ("Admin cannot directly reply... unless they take over").
- `reassignConversationAction` backs both "Transfer" (used by the current owner, gated by `COMMUNICATION_TRANSFER`) and "Reassign" (used by Admin or `COMMUNICATION_ASSIGN`, works regardless of current owner) — one action, permission checked based on who's calling it and whether they currently own the conversation.
- `takeOverConversationAction` lets Admin/`COMMUNICATION_ASSIGN` claim a conversation directly for when the responsible Staff member is unavailable.
- Every ownership change is recorded as an inline SYSTEM message and triggers a notification to the new assignee.

### Chat types and access control
New `ConversationParticipant` join table backs PRIVATE (Staff↔Admin, exactly 2 participants) and GROUP/CUSTOMER_GROUP (multi-party) conversations. Access rules, enforced in `hasStructuralAccess()`:
- Customers see only their own CUSTOMER/CUSTOMER_GROUP conversations.
- Staff/Admin see every CUSTOMER conversation (gated by `COMMUNICATION_VIEW`, matching the existing model) plus any PRIVATE/GROUP/CUSTOMER_GROUP conversation they're an explicit participant of — including from other Admins' private DMs, which are treated as genuinely private rather than covered by Admin's general oversight, since the spec explicitly frames them as confidential ("Other Staff must NOT see it").
- Staff can only start a private chat with Admin, not with other Staff directly (matches spec sections 11–13).

### Attachments and transaction references
`Message` gained attachment fields (path/name/mime/size, reusing the existing `saveUploadedFile`) and reference fields (`refType` + one of `refInquiryId`/`refQuotationId`/`refJobOrderId`). Validated server-side in `sendMessageAction`: 10MB size limit, an extension allowlist (images, PDF, Word, Excel, txt), and the referenced record must belong to the conversation's own customer. `MessageThread` renders attachments as inline image previews or file chips, and references as a structured, clickable card (type/number/status/amount where relevant) — matching the spec's mockup rather than plain text.

### Chatbox UI (`FloatingChatWidget`, `MessageThread`)
- Conversation list: kind badges (Customer/Private/Group/Customer Group), "Can Reply"/"View Only" for Staff/Admin, presence dots.
- Thread header (CUSTOMER type, Staff/Admin view): "Responsible: {name}" or "Unassigned", inline Transfer/Reassign/Take Over controls backed by a Staff search-and-pick list with live presence.
- "New Chat" panel (Staff/Admin only): Customer search (`COMMUNICATION_SEARCH_CUSTOMER`), Staff↔Admin private chat, Group creation (`COMMUNICATION_GROUP`).
- Composer: 📎 attach, 😊 emoji (a small fixed grid — deliberately lightweight, not a full picker), 🔗 reference-a-transaction, each gated by its own permission (`canAttach`/`canReference` props, defaulting `true` so Customer call sites keep full capability without extra plumbing).
- Removed: the old per-transaction "Messages" card on Inquiry/Quotation/Job Order pages, replaced by a `DiscussInChatboxButton` that opens the widget straight into the customer's central conversation with that transaction pre-referenced (dispatches a `chatbox:open-reference` window event the widget listens for).

### Presence, 24h reminders, automatic assignment
- **Presence**: `User.lastActiveAt`, updated by a client heartbeat every 30s while a Staff/Admin has the app open; online/away/offline derived at query time wherever Staff is listed — no push channel, "reasonable real-time" per the spec without new infrastructure.
- **24h no-response reminder**: `lib/response-reminders.ts` sweeps CUSTOMER conversations where the customer's been waiting 24h+ with no reply, notifying the responsible Staff (if any) and every Admin, deduped via `lastReminderSentAt` so it won't refire within the same 24h window.
- **Automatic assignment**: new `assignmentMode` on Business Settings (Manual / Automatic / Manual with a 15-minute fallback), `lib/auto-assignment.ts` picks the least-loaded eligible Staff member (`COMMUNICATION_VIEW`+`COMMUNICATION_SEND`, ranked online > away > offline) — Automatic fires inline when a customer's message lands in an unassigned conversation, the fallback mode via a sweep.
- **Honest infrastructure note**: this stack has no job queue or cron runner. Both sweeps ride opportunistically on SSE connections (debounced 10 min / 5 min respectively, `globalThis`-backed like the realtime bus) — that covers the common case with zero setup, and a dedicated `GET /api/cron/response-reminders` route (protected by an optional `CRON_SECRET` env var) exists for anyone who wants precise timing from a real external scheduler.

### Permissions
Six new Communication permissions (`COMMUNICATION_TRANSFER`/`ASSIGN`/`GROUP`/`ATTACHMENT`/`REFERENCE_TRANSACTION`/`SEARCH_CUSTOMER`), added to the `Permission` enum and to the existing preset templates (Sales Staff and Cashier get the customer-facing ones; Customer Service and Manager get everything). Admin bypasses all of them, same as every existing permission. Every action in `app/actions/messages.ts` checks the specific permission server-side before doing anything — the UI hides controls the caller can't use, but that's a convenience layer, not the enforcement.

### Verification
A full Playwright pass covering ownership/transfer/reassign/takeover, permission enforcement (including a replay-attack test — captured a legitimate Transfer request and replayed it with a different Staff account's session cookies; the DB stayed unchanged, confirming the server rejects it independent of the UI), private/group chat isolation, customer search, real-time delivery, attachments, transaction references, presence, the 24h reminder and auto-assignment sweeps, and a full regression smoke test — passed cleanly across the board, with two real bugs found in the attachments/references path:

1. **Live rendering gap**: attachments and transaction references sent while a conversation was open didn't render until the thread was reopened or the page reloaded — `createAndPublishMessage()`'s SSE broadcast only carried `{id, body, senderId, senderName, senderRole, messageType, createdAt}`, and `MessageThread`'s live-append path hardcoded `attachment`/`reference` to `null`. Fixed by extracting a `serializeMessage()` helper shared between the cold-fetch path (`getConversationMessagesAction`) and the live broadcast, so a message looks identical however it arrives.
2. **Oversized upload crash**: any attachment over ~1MB crashed with a raw Next.js "Body exceeded 1 MB limit" error instead of the intended graceful "That file is too large (10MB max)." message. Root cause took two passes: raising `serverActions.bodySizeLimit` wasn't enough — bisection testing (uploads from 8MB to 11MB) kept failing with a *different* error ("Unexpected end of form") at exactly 10MB no matter how high that limit went. The dev log had the real answer: `proxy.ts` (the auth middleware, matched on nearly every route) has its own independent default 10MB body cap (`experimental.proxyClientMaxBodySize` — the new name for `middlewareClientMaxBodySize` under the `proxy.ts` convention this project already uses), and a chat send passes through it before ever reaching the Server Action. Both limits are now raised with headroom above `ATTACHMENT_MAX_BYTES`.

Re-verified after the fix via direct bisection (not just re-running the original test): 9.9MB uploads succeed cleanly with zero errors, 11MB gets the graceful rejection message with zero page errors, and attachments/references render live on both sender and recipient sides without a reload. `npm run build` and `migrate diff --exit-code` both clean throughout.


---

## August 17 — update: Chatbox as the only messaging entry point, professional documents

### 1. Removed the standalone "Messages" sidebar
`nav-config.ts` dropped the "Messages" entry from all three role nav lists (Admin, Staff, Customer). The widget's own "View full message history" footer link (which pointed at `/messages`) was removed too — a grep across `app/` and `components/` after the change turned up no other navigation link pointing at the old page. The `/messages` and `/messages/[id]` routes themselves were left in place, unlinked rather than deleted — a lower-risk choice than removing working code nothing currently points at, and keeps a fallback available if ever needed.

### 2. Chat notifications open the Chatbox directly
Previously every chat-related notification (`NEW_MESSAGE`, `CONVERSATION_ASSIGNED`, `PRIVATE_CHAT_STARTED`, `GROUP_CHAT_CREATED`, `CHAT_RESPONSE_REMINDER`) carried a `/messages/{conversationId}` link and clicking it did a full-page redirect there via `openNotificationAction`. `NotificationBell` now branches on notification type: chat types skip the redirect entirely — they mark the notification read via the existing (non-redirecting) `markNotificationReadAction` and dispatch a `chatbox:open-conversation` window event carrying the conversation id parsed out of the link. `FloatingChatWidget` already had one listener for this exact pattern (`chatbox:open-reference`, from the "Discuss in Chatbox" button built in the previous update); a twin listener opens/re-opens the widget straight to that conversation, whatever page the user is currently on. Non-chat notification types are untouched — same form-submit-and-redirect as before.

### 3. Professional Quotation, Invoice, and Job Order documents
New `app/(print)` route group — deliberately outside the `(app)` Shell wrapper, so these pages have no sidebar/nav to fight with when printed (`app/(print)/layout.tsx` just enforces a logged-in session). A shared design system in `components/documents/`:
- `DocumentShell` — the header every document type shares: logo-or-letter-avatar, business name, tagline, address, contact, a divider, then the document title in brand red — all read live from `getBusinessSettings()`, so an Admin's changes to name/logo/address/contact propagate automatically with no hard-coded identity anywhere in these pages.
- `DocumentField` / `DocumentSection` — the labeled meta grids and section headers reused across all three documents.
- `DocumentStatusBadge` — border-plus-tint rather than a solid fill, since browsers inconsistently print background colors without an explicit opt-in; covers every status across all three document types plus the derived `UNPAID`/`PARTIALLY_PAID`/`PAID` labels the Invoice needs (no such field is stored anywhere — computed from amount paid vs. total).
- `DocumentItemsTable` / `DocumentTotals` — shared by the Quotation and Invoice (the Job Order has no pricing at all, so it doesn't use these): wrapping long descriptions, right-aligned `tabular-nums` currency, a visually emphasized Grand Total, `break-inside-avoid` so totals/approval blocks don't split across a printed page.
- Print CSS: a single `@page { size: A4; margin: 14mm 12mm; }` rule in `globals.css`. Repeating table headers and reasonable row-splitting come for free from using plain semantic `<table><thead><tbody>` markup — no extra CSS needed for that. `PrintButton` triggers the browser's native print/Save-as-PDF (`window.print()`) — no PDF-generation dependency.

**Quotation** (`/quotations/[id]/print`): quote number, dates, status, customer, contact (pulled from the linked User's email/phone), Prepared By, items table, totals, and an approval section (Prepared By + Customer Approval with the current status and an "as of" timestamp — there's no dedicated `approvedAt` field, so `updatedAt` is used and labeled honestly as an approximation). A "View Document" button on the existing interactive quotation page opens it in a new tab.

**Invoice** (`/orders/[id]/invoice`): no separate Invoice model exists in the schema — this is derived entirely from the existing Order + Payment data, the same way the interactive app already treats an Order as the record carrying payment state. Amount Paid sums CONFIRMED payments across every method via the existing `confirmedPaymentTotal()` helper, never assuming GCash specifically. Items come from the linked Quotation's line items when one exists; a walk-in order created without a quotation falls back to a single summary line built from the order's own total, so the invoice stays internally consistent either way. A "View Invoice" button on the order page opens it.

**Job Order** (`/job-orders/[id]/print`): structurally different from the other two by necessity — a Job Order carries no pricing (that lives on its Quotation), so there's no items table or totals section, matching the spec's own field list for this document type. Two fields needed a pragmatic source since nothing stores them directly: "Prepared By" comes from the `JOB_ORDER_CREATED` AuditLog entry (JobOrder itself has no `createdById`), and "Assigned Staff" comes from the *current stage's* `JobOrderStageLog.assignedTo`, since assignment in this app is per-production-stage, not per-job-order. A "View Document" button on the job order page opens it.

**Two spec-vs-schema gaps, handled the same way as prior updates** (structurally present in the layout, gracefully empty rather than inventing a new feature): the requested "Discount" column has nowhere to read data from — no discount concept exists anywhere in the schema, quotation totals are always exactly `qty × unitPrice` — so it renders as "—" on every row; and `Customer` has no address field at all, so "Address" renders as "—" everywhere it's requested. Both are flagged here rather than silently worked around.

### Verification
A full Playwright pass covering nav removal, the notification-opens-Chatbox behavior (including switching an already-open widget to a different conversation, and confirming non-chat notifications still redirect normally), all three document pages (content correctness cross-checked against the interactive pages, print-CSS with no sidebar, mobile viewport, branding consistency, direct-URL authorization), and a full regression smoke pass — passed cleanly with zero console errors and no blocking issues:

- **Nav removal**: confirmed absent from desktop sidebar and mobile hamburger for Admin/Staff/Customer; the widget's old footer link is gone; `/messages` itself still loads (intentional unlinked fallback) but nothing in normal navigation points at it.
- **Notifications**: a full round-trip (Customer→Staff and Staff→Customer) confirmed the Chatbox opens to the correct conversation with zero page navigation; a widget already open on a different conversation correctly switches when a new notification is clicked; a non-chat notification (quotation sent) still does its normal full-page redirect, unchanged; an Admin reassignment notification opened the recipient's Chatbox with the full history including the inline "Conversation reassigned…" system message.
- **Documents**: Invoice's Amount Paid/Outstanding/Payment Status cross-checked exactly against the interactive order page; Job Order document correctly omits the items table/totals section; all three share consistent Red+White+Montserrat branding and header/footer structure; a customer attempting another customer's quotation-print URL directly was redirected, confirming this is enforced server-side and not just hidden in the UI.
- **Regression**: every existing surface (Inquiries, Quotations, Orders, Payments, Production, Inventory, Rewards, Staff Permissions, Audit Log, Business Settings, both dashboards, login/logout for all roles) still returns clean with no console errors; the Chatbox's ownership/transfer/reassignment/attachments functionality from the prior update was implicitly re-exercised throughout the notification tests with no regressions.

One minor, non-blocking note: on mobile, the Quotation/Invoice items table scrolls within its own container (by design — `overflow-x-auto` on a table wider than the viewport) rather than staying fully visible at a glance; this is a self-contained scroll region, not a page-level layout break, and all columns remain reachable.

---

## August 17 — 2nd update: Customer Records, Kanban Production, Transaction Summary, and login-free public tracking/sharing

The largest single update this project has taken — eight roughly-independent workstreams, each committed and pushed as its own phase per the session's commit-frequently discipline. Architecture decisions below, by workstream.

### 1. Customer Records independent of login accounts
`Customer` gained real `displayId` (`CUST-000001`-style, sequential, unique), `email`, `contactNumber`, `address`, and `facebookUrl` columns — previously contact info only existed indirectly via an optional linked `User`, and there was no address anywhere. The migration backfills a sequential `displayId` for every pre-existing row before locking the column to `NOT NULL UNIQUE`. `Customer.userId` was already nullable+unique from the original schema, so "a Customer Record with no login" needed no new nullability — just the missing contact fields.

- `searchCustomersForTransactionAction` (`app/actions/customers.ts`): multi-field search (name, company, email, contact number, Facebook, Customer ID, plus the linked User's name/email as the closest analog to "Username" — this app has no separate username concept, accounts log in with email) always sorted/labeled by Customer Name first. Empty query returns nothing; results capped at 10; never loads the full table.
- `quickAddCustomerAction`: creates a permanent Customer row with no password/login fields (Complete Name*, Address*, Contact Number*, Email, Facebook), auto-generates `displayId`, gated by `CUSTOMER_CREATE`.
- `activateCustomerLoginAction`: attaches a **new** `User` to an **existing** `Customer.id` inside a transaction — never creates a second Customer row. All prior Inquiries/Quotations/Orders/Payments/Rewards stay attached since they all reference the same `customerId` throughout. Gated by `CUSTOMER_EDIT`.
- New `/customers` (list + search) and `/customers/[id]` (detail, edit, Activate Login) pages, gated by the existing (previously dormant — no page had used them before) `CUSTOMER_VIEW`/`CUSTOMER_CREATE`/`CUSTOMER_EDIT` permissions.
- `CustomerPicker` (`components/customers/customer-picker.tsx`): the reusable search-as-you-type + "+" quick-add component, dropped into the Quotation and Order creation forms in place of the old `<Select>` that preloaded every customer.

### 2–6. Quotation/Invoice/Job Order preparation UI redesign
The Quotation and Order creation forms (`quotation-form.tsx`, `order-form.tsx`) were restructured into the same section language as the print documents — Customer Information (the new picker), Transaction Information (two-column grid), Items/Services (existing line-item editor, unchanged behavior), Notes — via a new client-safe `FormSection` component. `FormSection` is a deliberately separate file from `document-shell.tsx`: that file's `DocumentSection` looks identical but lives in a module that also top-level-imports `prisma` (via `DocumentShell`'s `getBusinessSettings()` call), and this codebase has already hit the bundler-boundary bug once this session where importing *any* export from such a module into a Client Component pulls the server-only chain into the client bundle. A new `BusinessInfoBanner` server component (reads `getBusinessSettings()` directly, distinct from `DocumentShell`'s full print header) sits above both forms.

`JobOrder` gained a real `productionInstructions` column, wired through `createJobOrderAction` and the Add Job Order form, and the Job Order print page now shows it as its own section distinct from the general `description` field (previously the print page mislabeled `description` as "Production Instructions").

**Scope decision, disclosed here rather than silently skipped:** the spec's Invoice UI item (#4) is about the *preparation* UI, but this app has no separate "create Invoice" flow — the Invoice *is* the Order (same as established in the previous update), generated from Order + Payment data, not authored directly. So there was no separate Invoice creation form to redesign; the Order form redesign is what stands in for it, and it already supported all existing payment methods before this update (via `confirmedPaymentTotal`, method-agnostic).

### 7. Kanban Production dashboard
`/production` was rebuilt from a flat table into `KanbanBoard` (`app/(app)/production/kanban-board.tsx`): columns are the union of configured `WorkflowStage` names across active `WorkflowTemplate`s, in sequence — reusing that existing architecture exactly as instructed, not a new stage system — plus a trailing "Ready for Fulfillment" column. Each card shows JO #, customer, service (`productType`), quantity, due date, status, and assigned staff, with "View JO" and a permission-gated "Start Stage" / "Approve for Next Stage →" action (the latter is the existing `completeStageAction`/`CompleteStageForm`, relabeled — not a new action). A client-side "Service" filter narrows the board by `productType`. Desktop shows columns in a horizontal row; they wrap on tablet width and stack full-width on mobile (`flex-col sm:flex-row sm:flex-wrap`).

`completeCurrentStage` (`lib/workflow.ts`) now also records an approver of record: if the stage log's `assignedToId` was never set (nobody explicitly "started" the stage first), completing it sets `assignedToId` to the approving actor. The existing customer notification on stage completion is unchanged, satisfying "trigger notifications" with no new notification path.

### 8. Transaction Summary dashboard + PDF
New `/reports/summary`, gated by the existing (previously dormant) `REPORTS_VIEW`/`REPORTS_EXPORT` permissions. `lib/transaction-summary.ts` is the single source of truth: `resolvePeriodRange()` turns a Daily/Monthly/Quarterly/Semi-Annual/Annual selection into a concrete `[start, end)` range (a date / a month / Q1–Q4 / First-or-Second-Half / a year, exactly per spec), and `computeTransactionSummary()` computes every metric (Inquiries, Quotations, Orders, Invoices, Payments, Sales/Revenue, Outstanding Balance, Cancelled, Completed) against that range, reusing the same Prisma models and `CONFIRMED`-payment semantics as `lib/workflow.ts`'s `paymentSummary()` and the existing Admin dashboard — not a parallel calculation system. Charts reuse the existing `components/dashboard/admin-charts.tsx` Recharts wrappers, plus one new `PaymentsByMethodChart`. "Generate PDF" opens `/reports/summary/print`, built with the same `DocumentShell` as the other documents, including a "Report Information" footer naming who generated it and when.

**Spec-vs-schema gap, disclosed:** there is no separate Invoice model, so "Total Invoices" is defined as the count of distinct Orders with at least one Payment recorded in the period (otherwise it would be a pure duplicate of "Total Orders").

### 9–16. Login-free Customer Order Tracking
New `OrderTrackingLink` model; `generateSecureToken()` (`lib/order-tracking.ts`) produces a 24-byte `crypto.randomBytes` base64url token — non-sequential, unguessable, and the only way to reach the page (the order's real `id` never appears in the public URL). A new `app/(public)/` route group has no `requireUser()` gate at all (unlike `(app)` and `(print)`) since there's no session for an anonymous visitor to authenticate.

`/track/[token]` shows only customer-safe fields (customer name, order number/date/status, payment status, outstanding balance where applicable, expected completion date pulled from the order's nearest upcoming `Fulfillment.scheduledDate`, current production stage) and a visual progress timeline (`buildOrderTimeline()`) built from the *real* Quotation status and JobOrder/WorkflowStage sequence — Order Received → Quotation → Approved → Job Order → the actual configured production stages → Completed — never internal staff notes, costs, other customers, or assignments. The page self-refreshes every 20s via a plain server action re-fetch, not a new SSE channel (there's no session to key a realtime subscription off of for an anonymous visitor — the existing SSE infrastructure is built around authenticated users). Revoked/expired links, and tokens that never existed, all render the same "no longer available" message so the response can't be used to distinguish a bad token from a revoked one.

Admin/authorized Staff (new `ORDER_TRACKING_MANAGE` permission) manage links from a panel on the Order detail page: Generate / Copy / Share (Web Share API where available, falling back to copy; direct `mailto:`/`sms:` links) / Revoke / Regenerate, with No-expiration / 7-day / 30-day / Custom expiry. Regenerate revokes any currently-active link before issuing the new one, so an old shared URL stops working immediately.

### 17–29. Secure document sharing (deliberately separate from tracking)
New `DocumentShareLink` model — its own table, own token space, own permissions (`DOCUMENT_VIEW`/`DOCUMENT_SHARE`/`DOCUMENT_DOWNLOAD`/`DOCUMENT_REVOKE`), own public route (`/documents/[token]`) — kept structurally independent from `OrderTrackingLink` per the spec's explicit instruction not to combine these into one system. It references exactly one of `quotationId`/`orderId`(=Invoice)/`jobOrderId`, mirroring `Message`'s existing `refQuotationId`/`refJobOrderId`-style polymorphic-by-nullable-FK pattern rather than inventing a new polymorphic-association convention.

A "Share Document" panel (same Generate/Copy/Share/Revoke/Regenerate UX as the tracking-link panel, intentionally a visually-similar-but-separate component, `DocumentShareManager`) appears on the Quotation, Invoice (Order), and Job Order detail pages. Admin/authorized Staff choose View Only vs. View + Download PDF and an expiration at generation time. A logged-in Customer gets the same panel in a reduced form (no access-level choice) and may only share their **own** transactions — enforced in `generateDocumentShareLinkAction` via `ownerCustomerIdForDoc()` (checks the document's actual owning Customer against the caller's own Customer record), not merely by which button the frontend renders; a Customer's share is also hard-coded to `VIEW_ONLY` server-side regardless of what a forged request claims.

The public page reuses the exact same `DocumentShell`/`DocumentField`/`DocumentItemsTable`/`DocumentTotals` components as the internal print pages, so a shared document looks identical to the real one. View-Only is enforced two ways, not just by omitting a button: (1) the Download button is only rendered when `accessLevel === VIEW_DOWNLOAD`; (2) `recordDocumentDownloadAction` re-validates `accessLevel`/expiry/revocation server-side before incrementing the download counter or letting the client proceed to `window.print()` — a forged direct call to this action still gets rejected for a View-Only link. A View-Only document's real content is also wrapped in `print:hidden`, with a `hidden print:block` placeholder ("This document is view-only and cannot be printed or downloaded...") shown instead — so a bare Ctrl+P doesn't produce a clean copy either. This is disclosed honestly as real, meaningful technical enforcement, not absolute prevention: no browser-based system can stop a screenshot.

Every link (both types) tracks `viewCount`/`lastViewedAt` and (documents only) `downloadCount`/`lastDownloadedAt` as its own audit trail, visible wherever the manager panel is shown.

### A bug this phase caught and fixed: public routes were still behind the login wall
`proxy.ts` — the auth middleware matched on nearly every route — redirects any request with no session straight to `/login`, with only `/login`/`/register` (plus Next internals) as exceptions. Both new `(public)` routes were **not** in that exception list, meaning an anonymous visitor hitting a shared tracking or document link would have been bounced to a login page instead of seeing the page — silently defeating both "login-free" features. Caught before shipping by testing with plain unauthenticated `curl` (no cookies) rather than only testing through an already-authenticated browser session: `/track/<token>` and `/documents/<token>` returned Next's redirect status until `/track/` and `/documents/` were added as public path-prefix exceptions in `proxy.ts`, re-verified afterward with the same unauthenticated `curl` requests (200, not a redirect) alongside a still-gated authenticated-only route as a control.

### Other schema/permission additions
`Permission` enum gained a `Documents` category (`DOCUMENT_VIEW`/`SHARE`/`DOWNLOAD`/`REVOKE`, `ORDER_TRACKING_MANAGE`), added to the Sales Staff and Customer Service presets alongside `CUSTOMER_VIEW`/`CUSTOMER_CREATE`/`REPORTS_VIEW` (needed for those presets to coherently cover the new Customer-search and reporting surfaces they now touch). New `/customers` and `/reports/summary` nav entries added for Admin/Staff (permission-gated for Staff).

**Closed a previously-disclosed gap while in this code:** the Quotation/Invoice/Job Order print pages (built in the 1st update, before `Customer.address`/`email`/`contactNumber` existed) always rendered "Address" as "—" and derived contact only through the linked `User`. They now read the real `Customer` fields first, falling back to the linked `User`'s email/phone only for older records that predate this update.

### Known gaps and deliberate scope decisions, disclosed
- **No reference screenshot was actually visible** for the "professional invoice-generation interface" mentioned in the spec — built from the detailed text description only, same situation as the 1st update's reference image.
- **"Username" search** has no real field to search — this app authenticates by email, there's no separate username concept anywhere in the schema. The linked User's name/email is searched as the closest analog.
- **JobOrder has no JO-level "Assigned Staff" field** — assignment is per-production-stage (`JobOrderStageLog.assignedToId`), not per-job-order. Kanban cards and the print document both show the *current stage's* assignee as a proxy, consistent with how the print page already handled this in the 1st update.
- **Kanban columns are name-matched, not ID-matched** across templates — if two `WorkflowTemplate`s used different names for what's conceptually the same stage, they'd appear as separate columns. No stage-name aliasing/normalization system was built.
- **Multiple Job Orders per Order**: the public tracking timeline's production-stage section is driven by the *first* Job Order only.
- **The public tracking page's "Contact Us" is a static Business Settings block** (address/phone/email), not the internal Chatbox — an anonymous visitor has no session to attach an authenticated conversation to, so wiring in the existing Staff/Admin chat system wasn't technically appropriate for this specific surface. This was a deliberate judgment call under the spec's own "where technically appropriate" / "if not appropriate, use a lightweight alternative" language, not an oversight.
- **Discount column**: still no underlying data anywhere in the schema (unchanged from the 1st update) — still renders "—".

### Verification
A background Playwright pass covering all eight workstreams (94 screenshots, console/page-error monitoring on every page) found 7 of 8 areas clean on the first pass, plus two real bugs — both root-caused and fixed immediately afterward, then independently re-verified (Playwright + a direct DB check, against a freshly restarted dev server to rule out stale-process false negatives):

1. **Customer self-service document sharing silently did nothing.** The Customer-facing "Share Document" form omits the access-level picker (self-service is always View Only), so `formData.get("accessLevel")` came back `null` — but Zod's `.optional()` only accepts `undefined`, not `null`, so validation failed and the action returned early with zero feedback to the user. Fixed by normalizing to `|| undefined` before parsing (`app/actions/document-sharing.ts`), matching the pattern already used for the optional `customDate` field. Re-verified: a logged-in customer sharing their own quotation now creates a real `VIEW_ONLY` `DocumentShareLink` row, confirmed both in the UI (link input populated, Revoke button present) and directly in the database.
2. **The inline "+" quick-add customer inside the Quotation/Order preparation forms was broken.** `QuickAddCustomerModal` renders its own `<form>`, and `CustomerPicker` (which renders that modal) is itself used inside `quotation-form.tsx`/`order-form.tsx`'s outer `<form>` — a `<form>` nested inside another `<form>` is invalid HTML and silently breaks submission (confirmed via a React hydration warning and "form was unexpectedly submitted" console errors). The standalone `/customers` page's own quick-add button was unaffected since it isn't nested inside another form. Fixed by rendering the modal through a `createPortal` onto `document.body`, so its form is never a DOM descendant of whatever form happens to contain `CustomerPicker`. Re-verified: quick-adding a customer from inside the Quotation form now creates and auto-selects the customer with zero console errors.

Everything else verified clean: Customer Records search/quick-add/Activate Login (including confirming no duplicate Customer row was created); the Business Info/Customer/Transaction/Items section layout on both prep forms, including the customer field correctly locking read-only for an order created from a quotation; Production Instructions saving and rendering in its own section on the Job Order print page; the Kanban board's columns, Service filter, and "Approve for Next Stage" actually advancing a job order's stage (cross-checked against the stage history table) and staying usable at mobile width; the Transaction Summary period selector, metrics, charts, and "Generate PDF" output; the full Order Tracking Link lifecycle (generate → view unauthenticated with no internal data leaked → revoke → confirm "no longer available" → the 20s background poll running without errors); the full Document Share Link lifecycle for View Only (no Download button, and `print` media emulation confirmed the real content is replaced by the view-only placeholder rather than being printable) and View + Download (button present, download recorded, real content printable); and a full regression pass across all four roles with zero console errors, including confirming `Customer.address` now renders correctly on the print documents where it previously always showed "—".

Minor, non-blocking notes from the same pass, left as-is:
- `/customers` preloads the 25 most-recently-created customers by default rather than showing a fully empty list until a search is typed — a reasonable middle ground (an empty list feels broken on first visit) that still satisfies the core requirement of never loading the *entire* customer table; flagged here since the spec's literal wording was "never loading the full customer list."
- The Transaction Summary dashboard shows `0` for a zero-count metric while the PDF version shows `—` for the same case — a cosmetic formatting inconsistency between the two render paths, not a functional issue.
- At narrow mobile widths, the floating Chatbox button visually overlaps the corner of a Kanban card on `/production` — a z-index/layout nit, not a functional break.

---

## August 18 — 1st update: Statement of Account and the centralized Email Notification System

Two large, mostly-independent systems in one update. Architecture decisions below, by system.

### Statement of Account (SOA)

**Data model.** `StatementOfAccount` is a persisted *snapshot* — statement number, period, and the four summary totals (opening balance, charges, payments, adjustments, outstanding) — so a previously-issued statement stays stable for history/audit even as new transactions land later. The transaction-by-transaction breakdown table itself is never stored: `lib/soa.ts`'s `computeStatementOfAccount()` always recomputes it live from `Order`/`Payment`/`AccountAdjustment` records within `[periodStart, periodEnd)` at render time, reusing the exact same `Order.totalAmount`-as-charge-event and `CONFIRMED`-payment semantics as `paymentSummary()` and the Transaction Summary dashboard — never a parallel calculation system, per the spec's explicit instruction. An Order **is** the "charge"/invoice event in this app (same as the existing Invoice document already treats it) — there's no separate Invoice ledger to read from. New `AccountAdjustment` model (CHARGE/CREDIT, optional Order link) backs manual credits/write-offs, factored into every statement and into the current-balance snapshot everywhere it's shown.

**Due/Overdue status, honestly.** The spec is explicit: never mark something overdue without an actual due date that has passed. This app had no due-date concept anywhere before this update, so `Order` gained an optional `dueDate`, set at order creation (`app/(app)/orders/new/order-form.tsx`, wired through `createOrderAction`). `deriveSoaBalanceStatus()` in `lib/soa.ts` is strict: an order with no `dueDate` can only ever read CURRENT, never DUE or OVERDUE. Editing a due date after order creation isn't supported yet — a disclosed gap, not silently worked around.

**Numbering, lookup, and UI.** `nextStatementNumber()` (SOA-2026-08-0001-style, sequential within the statement's ending month) follows the same pattern as `nextOrderNumber`/`nextQuoteNumber`. Customer SOA Lookup (`/soa`) reuses the existing `CustomerPicker` component as-is — search by name/company/email/contact/Facebook/Customer ID, always Customer Name first — via a new optional `onSelect` callback added to `CustomerPicker` for callers (like this one) that want to navigate immediately on selection instead of waiting on a form submit. `/soa/customer/[id]` shows the customer's current total outstanding balance with a status badge, a Monthly-or-Custom-date-range statement generator, the Adjustments panel, a Monthly Statement Schedule panel, and previous statements. `/soa/view/[id]` shows Account Summary, the full transaction table, a prominently emphasized Total Outstanding, "Send SOA Email," "Discuss in Chatbox" (reuses the customer's existing conversation via `startCustomerConversationAction` — no separate messaging system), and "Generate PDF." `/soa/[id]/print` uses the same `DocumentShell` as Quotation/Invoice/Job Order/Transaction Summary, including the exact structure the spec specified (header → Statement Info → Customer → Account Summary → Transaction Details → Total Amount Due → Payment Instructions), reading Business Settings' new `paymentInstructions` free-text field rather than a hard-coded payment-methods list (no structured "business payment accounts" model exists, so this is a single editable block covering the actual requirement without inventing one).

**Monthly SOA management** (`/soa/monthly`) lists every customer with an outstanding balance for a selected month (`findCustomersWithOutstandingBalance()`, same underlying records), with Outstanding Balance/Last Payment/Overdue Amount/Status/SOA-Generated-or-not, "Generate SOA" per row, and "Generate All." Both call the same `createStatementAndNotify()` helper (`lib/soa-generate.ts`) the interactive flow uses, so numbering/notification/audit behavior can never drift between the three entry points (interactive, monthly-dashboard, and the recurring sweep below). "Generate All" never sends anything automatically — Send/Share is still a separate, explicit click per statement.

**Recurring SOA schedules** (spec's "Future SOA Automation"): new `StatementSchedule` model, per customer — day-of-month, "only if outstanding" condition, `enabled` (defaults `false`, requiring a separate explicit "Enable" click after creation, per the spec's "configurable and explicitly enabled by Admin"). `runStatementScheduleSweep()` follows the exact same no-job-queue pattern already established in this codebase for the 24h chat reminder and auto-assignment fallback: a debounced opportunistic trigger off every SSE connection (`app/api/realtime/route.ts`), plus a dedicated `GET /api/cron/statement-schedules` route (same `CRON_SECRET`-gated shape as the existing response-reminders cron route) for anyone wiring up a real external scheduler. A due schedule generates and emails the *previous* calendar month's statement.

**SOA sharing reuses the document-sharing architecture exactly**, not a parallel system: `DocumentShareLink` gained a fourth polymorphic FK, `statementId`, alongside the existing `quotationId`/`orderId`/`jobOrderId` — same secure random token, View Only vs. View + Download (enforced server-side, not just a hidden button), expiration, revoke/regenerate, and audit counters (view/download counts and timestamps). The same public `/documents/[token]` viewer now renders a fourth document body (`StatementBody`) alongside Quotation/Invoice/Job Order. New `SOA_VIEW`/`SOA_GENERATE`/`SOA_SHARE`/`SOA_REVOKE` permissions replace the generic `DOCUMENT_SHARE`/`DOCUMENT_REVOKE` checks specifically for SOA links (`assertCanShare` branches by doc type). **Bug caught and fixed during this work**: the existing docType-derivation logic in `revokeDocumentShareLinkAction` and `resolvePublicDocument` (`link.quotationId ? "QUOTATION" : link.orderId ? "INVOICE" : "JOB_ORDER"`) silently fell through to `JOB_ORDER` for any link that only had `statementId` set — both now check `statementId` explicitly before falling through, closing a real misauthorization risk this update's own schema change introduced.

**Customer access**: the Customer Payments page (`/payments`, customer's own view) gained a Statement of Account section — statement number/period/outstanding balance per prior statement, linking to its PDF — reached via Payments exactly as the spec's navigation path specifies, sitting alongside the existing balance-by-order and payment-history tables (neither of which was touched). Login-free customers reach their SOA the same way they reach any other shared document: the secure public link, no account required.

### Centralized Email Notification System

**The single funnel.** Rather than touching the ~25 existing call sites across `app/actions/**` that already produce a bell notification, `lib/notifications.ts`'s `notifyUser`/`notifyCustomer`/`notifyStaff` — already called from every one of them — now also check the email master switch, the per-event override, and a real recipient address, then render a template and queue+attempt an email, all in one place. This is exactly the "Business Event → Notification Service → checks Email Master Switch / Individual Event Setting / Customer Email / Template → Email Queue → Configured Email Provider" architecture the spec describes, and it means every existing event that already notifies (Quotation Sent/Approved/Rejected, Payment Confirmed/Rejected, Order Created/Completed, Job Order Completed, Production Progress, Fulfillment updates, Rewards/Vouchers, the existing Balance Reminder, plus the new SOA Generated/Shared/Payment-Reminder and Document Shared events) is email-capable with zero duplicated logic. A handful of purely-internal notification types (chat activity, the 24h chat reminder, a design-draft-uploaded ping) are deliberately excluded from ever emailing — see `NON_EMAIL_TYPES` in `lib/email-events.ts` — email is an additional channel, not a replacement for the Chatbox, and not every internal ping needs to leave the app.

**A previously-silent gap this closed**: `notifyCustomer()` used to skip walk-in customers with no linked `User` entirely (no in-app bell target existed for them). It now still can't give them a bell notification (there's genuinely no `User` row to attach one to), but sends email directly via `Customer.email` when present — so a login-free customer can now actually receive their SOA or a payment confirmation, which the spec requires and the old code silently couldn't do.

**Provider/credentials.** `lib/email.ts` builds a `nodemailer` transport: Gmail/Yahoo/Outlook use fixed host/port (Admin only supplies the mailbox + password), Custom SMTP reads full host/port/security from Business Settings. The SMTP password is AES-256-GCM encrypted at rest (`lib/email-crypto.ts`), keyed off the existing `AUTH_SECRET`/`NEXTAUTH_SECRET` rather than requiring a brand-new env var most deployments would forget to set — never returned by any read path, never logged. **Honest limitation, disclosed rather than faked**: real OAuth consent flows for Gmail/Microsoft are not implemented — this app has no way to register an OAuth client with those providers on its own, and fabricating one would be worse than not offering it. Authentication is via App Password / SMTP credentials, one of the mechanisms the spec explicitly lists as acceptable, and the UI labels each provider's password field accordingly ("use an App Password, not your normal password").

**Queue and log are the same table.** `EmailLog` (QUEUED → SENDING → SENT/FAILED) doubles as both, per the spec's own framing — there's no separate queue table to keep in sync with a log. The fully-rendered subject/body is stored on the row itself, so a failed send can be retried with the *exact* original content rather than an approximation. Retry is manual, one attempt per click, left to Staff/Admin judgment (spec: "do not retry invalid addresses indefinitely") — gated by a new `EMAIL_LOG_VIEW` permission so it can be granted to Staff, not just Admin.

**Templates.** `EmailTemplate` holds Admin-editable `{{variable}}` subject/body per event key, falling back to a sensible built-in default (`lib/email-events.ts`) when uncustomized — Admin only needs to create a row to override one event, not maintain all of them. The full spec variable list is supported (`customer_name`, `statement_number`, `balance`, `soa_link`, `business_name`, etc.), plus one practical addition beyond the spec's own list — `{{message}}` — since the centralized notification path (by design) doesn't have access to every event's fully-structured data (an `{{amount}}` or `{{quotation_number}}` specifically) without touching each of the ~25 call sites individually; the generic default template instead surfaces the same human-readable message text already shown in the bell notification. The SOA-specific templates (which *are* authored with full structured data, since those call sites are new code written for this update) use the richer variables properly.

**No automatic sending without configuration.** `emailEnabled` defaults `false` on `BusinessSettings`; per-event overrides live in a JSON map (`emailEventSettings`) where an absent key means "on" once the master switch and a provider are both configured, and an explicit `false` overrides it off. Turning email off never touches bell notifications, the Chatbox, or any transaction — `sendEmailEvent()` no-ops safely at every gate (wrong/unknown type, master switch off, event disabled, no recipient address) and never throws, so a misconfigured or unreachable provider can never break the business action that triggered it.

**Admin UI.** Business Settings → Email Settings (master switch, provider/credentials form, Test Email Connection with a real `transporter.verify()` + a real test send — only reports success if the provider actually accepted it, failure otherwise with the underlying error surfaced) — plus per-event ON/OFF toggles grouped by category, and a link to Email Templates. Business Settings itself also gained the `paymentInstructions` field the SOA/Invoice documents read.

### Verification
A background Playwright pass covering both systems (SOA generation/view/PDF/sharing/monthly-dashboard/schedules/customer-portal-access, and Email settings/provider-config/test-connection/templates/log/master-switch-off-behavior) plus a regression check of the previously-existing Quotation/Invoice/Job Order sharing (touched indirectly by this update's changes to `document-sharing.ts`/`public-document.ts`) came back clean on every item except one, which was fixed immediately after and independently re-verified:

**Bug found and fixed**: the order creation form and `createOrderAction` never actually exposed a way to set `Order.dueDate` — the field existed in the schema and `lib/soa.ts`'s Due/Overdue derivation logic was already correct (confirmed working by the verification agent via a direct database edit), but there was no UI path to it, so OVERDUE could never occur in real usage. Root cause: this exact fix had been made once already earlier in this session but was lost to an environment reset before it was committed — a reminder of why every phase in this update was committed immediately rather than batched. Re-applied and this time verified live end-to-end (not just via build): a "Payment Due Date" field now appears on `/orders/new`, creating an order with a past due date persists correctly to the database, and the customer's Statement of Account genuinely shows OVERDUE afterward — while an order with no due date set correctly stays CURRENT, honoring the spec's "never overdue without an actual due date" rule.

Everything else verified clean: full SOA lifecycle (lookup → customer page → adjustments → schedule creation/enable → monthly and custom-period generation → transaction-table arithmetic → PDF structure → View-Only vs. View+Download sharing in fresh unauthenticated contexts → Monthly SOA dashboard including "Generate All" → customer-portal access); the full Email system (master switch on/off, provider config persisting correctly, Test Email Connection failing gracefully against an unreachable host in under a second, per-event toggles, template editing showing Default→Customized, Email Log with a genuine FAILED row and a no-crash Retry, and — critically — a business transaction completing normally with the bell notification still firing while the email master switch was OFF); and a regression pass across all three roles plus the pre-existing Quotation/Invoice/Job Order print and sharing surfaces, with zero console errors anywhere in the run.

## August 18 — 2nd update: Service Master, information carry-over, public order tracking, OAuth sign-up, and Messenger notifications

The largest single update of the project so far — five mostly-independent systems, all built on top of (never duplicating) the Customer/Quotation/Order/Job Order/Payment/Notification/Email/Chatbox/Document-sharing/Authentication/Business Settings architecture already in place. Architecture decisions below, by system.

### Service/Product Master

**Reuses `WorkflowTemplate`/`WorkflowStage` as its production flow**, rather than inventing a second stage-management system — `Service.workflowTemplateId` is just a pointer into the workflow-templates admin UI that already existed (add/rename/reorder/QC-flag stages). A new `Service` model (name, description, category, `active`, `workflowTemplateId`, `specFields: Json` — a flat array of admin-defined label strings like `["Width","Height","Material","Finishing"]`) is the single source of truth for what services exist and their default production flow, with full CRUD at `/admin/services` gated by new `SERVICE_VIEW`/`SERVICE_MANAGE` permissions.

**Snapshot vs. live-link, applied consistently everywhere a transaction references a Service**: `Inquiry`, `QuotationLineItem`, and `JobOrder` each kept their existing free-text field (`desiredProduct`/`productType`) as an immutable snapshot populated at creation time, and each gained a new nullable `serviceId` FK purely for the live link/carry-over/reporting. Renaming or deactivating a Service in the Master never changes a historical document's displayed product name — satisfying the spec's explicit "historical documents must never unexpectedly change" alongside its equally explicit "encode once, carry forward" requirement, which would otherwise be in tension.

**`ServicePicker`** (`components/services/service-picker.tsx`) replaces every free-text product input across Inquiry, Quotation line items, and Job Order creation: debounced search-as-you-type against only `active` services, a selected-state chip, and a permission-gated "+" quick-add (`QuickAddServiceModal`, rendered via `createPortal` onto `document.body` — the same nested-`<form>` fix already established for `QuickAddCustomerModal`, applied proactively here since the pattern was already known). Inactive/archived services are excluded from every search result server-side (`searchActiveServicesAction`), never client-filtered, so there's no way to select one that shouldn't be selectable. A `SpecFieldsEditor` renders one optional text input per the selected service's configured `specFields` and serializes answers into a single hidden JSON field — nothing is mandatory, matching "Service Master should allow Admin/authorized Staff to configure appropriate information," not force every field on every service.

**Carry-over, concretely**: creating a Quotation from an Inquiry pre-fills the first line item's service, specs, description, and quantity from that Inquiry (`/quotations/new/page.tsx`); creating a Job Order from an Order pre-fills the service, specs, quantity, and description from the linked Quotation's first line item, and separately auto-selects the Job Order's production-flow dropdown from that service's configured `workflowTemplateId` (still overridable by staff) — so "the system automatically loads the service's production flow" as specified, without removing staff's ability to choose a different flow for an edge case. All three server actions (`createInquiryAction`, `createQuotationAction`/`editQuotationAction`'s `parseLineItems`, `createJobOrderAction`) re-resolve the submitted `serviceId` against the live, active Service table themselves rather than trusting client state, and reject unknown/inactive services — the client-side prefill is a convenience, not the authorization boundary.

### Public order tracking: homepage, reference-number lookup, and extended tracking links

**The public homepage** (`app/page.tsx`) previously force-redirected every anonymous visitor to `/login`; it's now a real landing page — business branding, a prominent "Track Your Order" section, Sign In / Create Account links — while signed-in visitors still redirect straight to their role's dashboard exactly as before. `proxy.ts` gained `"/"` as an exact-match public path (the same gap pattern already fixed once for `/track/*` and `/documents/*` in an earlier update) — verified live via an unauthenticated `curl` returning 200, not a redirect.

**Reference-number lookup without a token.** The existing `/track/[token]` link (introduced in the Aug 17 2nd update) is secure because the token itself is unguessable and unique per order. A bare reference number like `ORD-2026-0007` is short and sequential, so the new `lookupTrackingByReferenceAction` (`app/actions/reference-lookup.ts`) requires the customer's own email or phone on file as a second factor before returning anything — the same "reference + something only the owner would know" pattern common to airline/courier tracking. It resolves Order, Quotation, and Job Order reference numbers to the right transaction (Job Order numbers are only unique *within* their parent Order, so a match there is validated against every candidate order and only returns a result when exactly one passes the contact check — anything ambiguous fails closed rather than guessing). A Quotation reference with no Order created yet still returns a customer-safe status message instead of a hard failure. Every failure path — wrong reference, wrong contact, or a best-effort in-memory rate limit on repeated guesses against one reference — is intentionally indistinguishable from the outside, so the endpoint can't be used as an oracle to enumerate valid references or contact info. The rendered result reuses the exact same `buildPublicSnapshot()`/customer-safe payload shape as the token-based page (extracted into a shared `TrackingSnapshotCard` component so both entry points render identically without duplicated markup) — never a second, parallel tracking data source.

**Tracking links extended to Job Order and Quotation detail pages** (previously Order-only): both now embed the same `TrackingLinkManager` (Generate/Copy/Share/Revoke/Regenerate), pointed at that Job Order's or Quotation's linked Order — tracking is inherently order-level (the timeline already spans Quotation → Job Order → Production → Delivery for one order), so this reuses the existing `OrderTrackingLink` model and actions rather than creating per-document tracking.

**Live, service-specific timelines — this was already correct, not newly built.** `lib/order-tracking.ts`'s `buildOrderTimeline()` reads the actual `JobOrder.workflowTemplate.stages` for whichever workflow template that specific Job Order is using — since the Service Master wiring above makes every new Job Order's template selection derive from its Service by default, the public tracking page already shows a genuinely different stage sequence for a Tarpaulin order vs. a Uniforms order with no additional tracking-side work required. The public snapshot was extended with `service`/`jobOrderNumber`/`quantity` fields (customer-safe order info) and a simple Deposit/Balance-remaining note when payment is outstanding.

**UI polish against a user-supplied reference screenshot** (a third-party tracking page): copy-to-clipboard on the order number, DONE/CURRENT pill badges per timeline step with the current step visually emphasized (colored left-border card), a Last Updated timestamp, and a "Back To Home" action — added to the shared `TrackingSnapshotCard` so both `/track/[token]` and the homepage lookup picked it up at once. Kept deliberately in this app's existing Red + White + Montserrat system rather than the reference's purple branding, per the spec's own instruction that such screenshots are layout/UX reference only.

### Google/Facebook customer sign-up

**No adapter tables needed.** The app already uses next-auth v5 with JWT sessions and only a Credentials provider — there was no `PrismaAdapter` actually wired in despite the dependency being present, and no `Account`/`Session`/`VerificationToken` models. Adding Google/Facebook providers therefore didn't require any of that; `User.passwordHash` became nullable (new migration) so an OAuth-only account simply has none, and the Credentials provider's `authorize()` now explicitly rejects a null-hash account rather than crashing on `bcrypt.compare`.

**Google/Facebook are only registered as providers when real credentials are configured** via env vars (`GOOGLE_CLIENT_ID`/`SECRET`, `FACEBOOK_CLIENT_ID`/`SECRET`) — absent here, so `/login` and `/register` correctly show no OAuth buttons at all, with plain email/password completely unaffected either way. On a real OAuth sign-in, `lib/auth.ts`'s `jwt` callback (not the edge-safe shared `auth.config.ts`, which stays prisma-free for `proxy.ts`'s sake) resolves the internal `User` by the provider's verified email.

**Existing-customer linking, fixed for both paths, not just OAuth.** A new `lib/customer-linking.ts` helper (`linkOrCreateCustomerForUser`) attaches a brand-new login to any pre-existing login-free `Customer` record with a matching email — created earlier via Staff/Admin's Customer Records "+" quick-add — instead of creating a duplicate, preserving that Customer's id, displayId, and every Quotation/Order/Job Order/Payment/SOA/Reward already tied to it. This was silently missing from plain email/password registration too (it always created a brand-new Customer, never checked for an existing login-free one) — fixed there as well, since the spec explicitly requires this to work "so a Staff/Admin-created login-less Customer can later attach Google, Facebook, or normal registration to their existing record," and there was no reason the fix should only apply to the new OAuth path. Verified live via direct database inspection: registering with an email matching a pre-created login-free Customer reuses that same Customer id/displayId rather than creating a second row.

### Facebook Messenger notifications (honest stub, real Send API)

Mirrors the Email system's shape exactly, as the second channel through the same funnel: a master switch (default OFF), four category toggles (Production/Payment/Delivery/SOA-reminder — coarser than email's per-event granularity, matching the spec's own grouping), a `MessengerLog` queue+log table, and dispatch centralized in `lib/notifications.ts`'s `notifyCustomer` — every one of the ~20 existing call sites across payments/fulfillment/quotations/SOA/orders becomes Messenger-capable automatically, with zero per-module changes. Unlike email (which only reaches login-free customers via their `Customer.email`), Messenger applies uniformly to customers with or without a login, since Messenger opt-in never required an account in the first place.

`lib/messenger.ts` calls the **real** Meta Graph API Send endpoint and generates a **real** `m.me/<pageId>?ref=<code>` opt-in deep link — never a bare Facebook page URL, which the spec explicitly forbids as a simulation. A customer must tap that link and message the Page before anything is ever sent to them (`MessengerConnection.connected`); until they do, or until Admin supplies a real Page ID + Page Access Token, every attempt is logged `MessengerLog` status `SKIPPED` with a specific, honest reason rather than silently dropped or faked as sent — verified live by enabling the master switch and sending a quotation to an unconnected seeded customer, which produced exactly that SKIPPED row with the underlying quotation-send transaction completing normally regardless. The webhook (`app/api/messenger/webhook`) handles Meta's `hub.challenge` verification handshake and records each customer's PSID from their opt-in postback, and verifies `X-Hub-Signature-256` against an optional encrypted App Secret when one is configured. Admin gets `/admin/messenger-settings` and `/admin/messenger-log` (gated by the existing `EMAIL_LOG_VIEW` permission, reframed as covering both notification-delivery logs rather than adding a near-duplicate grant); customers see a "Connect Messenger" card on their dashboard once Admin turns the channel on. The existing Chatbox is completely untouched — it remains the sole internal communication system; Messenger is purely an external notification channel layered on top of the same event stream Email already uses.

### Email event gaps filled

Two events from the spec's notification list had no `notifyCustomer` call anywhere: creating a Job Order, and starting production on one. Both now notify (bell + email if enabled + Messenger if connected) and are registered in `EMAIL_EVENTS`/`MESSENGER_EVENTS` for individual Admin control. "Payment Required" and "Order Ready" were already effectively covered by the existing `ORDER_CREATED` and the final-stage `PRODUCTION_STAGE_UPDATE` message ("...finished production and is ready for release") respectively, so no new near-duplicate event type was added for those.

### Verification
A background Playwright pass covered all five systems above end to end against real seeded data — Service Master CRUD and its searchable dropdown (including confirming a deactivated service correctly disappears from search results), the full Inquiry → Quotation → Job Order carry-over chain, the public homepage and reference-number lookup (correct match, wrong-contact rejection, a non-globally-unique Job Order number correctly disambiguated by the contact check, tracking links generated from Order/Job Order/Quotation pages, and a bogus token correctly showing no data), OAuth-buttons-correctly-absent plus a full register/logout/login cycle, Messenger Settings/Log, and a general regression pass across every major role and page (Quotations, Orders, Job Orders, Payments, Production, Customers, Inventory, SOA, and a live payment recording). Every item passed except one, found and fixed immediately after:

**Bug found and fixed**: the Service Master rewrite of `LineItemsEditor` (Quotation line items) replaced the old hidden `description`/`qty`/`unitPrice` inputs with directly-named visible inputs, but the `name` attributes were never actually added to those three fields — only the `ServicePicker`'s `serviceId` and the `productType`/`specs` hidden inputs were being submitted. Every "Save as Draft" or line-item edit therefore silently submitted `description`/`qty`/`unitPrice` as `undefined`, which the Zod schema in `createQuotationAction`/`editQuotationAction` always rejected — blocking the entire "New Quotation" and "Edit Quotation" flow with an "Invalid input" error and no way to proceed. This is exactly the kind of regression the "commit immediately, verify immediately" discipline exists to catch quickly: the verification agent isolated it precisely (reproduced the Zod error live, confirmed every downstream step — Send, Approve, Create Order, Add Job Order with carry-over — worked correctly once given a quotation inserted directly into the database, ruling out any broader problem). Fixed by adding the three missing `name` attributes and re-verified live end-to-end: a quotation created through the actual UI (customer picked via search, service picked via search, description/qty/price typed in) now saves correctly, redirects to the quotation detail page, and the database row shows the correct `description`/`qty`/`unitPrice`/`serviceId`.

Everything else verified clean, including a redeploy-safety check after the fix (clean git status, zero Prisma migration drift, `next build` succeeding with Postgres stopped to simulate a Docker build-before-DB-up deploy).

## August 18 — 3rd update: email root-cause fix, notification deep-link bug, Kanban redesign

Framed as "unfinished requirements from the previous updates," but inspection first showed most of the flagged systems (public tracking page, homepage tracking, extended tracking links, Google/Facebook OAuth scaffolding, Service Master carry-over) were already built and intact in the codebase — almost certainly a stale-deployment gap rather than a code gap, since this branch's Render deploy predates most of the 2nd update's commits. Per "inspect first, do not create duplicate systems," none of that was rebuilt. Four genuine gaps/bugs were found and fixed instead.

### Email — root cause, not another timeout

The first pass (previous commit) added SMTP connection timeouts, which was necessary but not sufficient — the spec explicitly called this out: "do not simply increase the timeout... the connection test must actually work." Digging further surfaced three real, separate problems layered under the same symptom:

1. **"Test Email Connection" was secretly "Send Test Email."** The button's label promised a connection-only check, but the action it called did `transporter.verify()` *and* `transporter.sendMail()` in one step, requiring a recipient address just to check whether SMTP auth worked. Split into two genuinely separate functions/actions/buttons — `testEmailConnection()` (verify only, sends nothing) and `sendTestEmail()` (actually delivers one message) — matching the spec's explicit "Test Connection vs. Send Test Email" distinction.
2. **Failures were reported as "Unknown error."** `classifyEmailError()` now maps nodemailer's real error codes to specific, actionable text — `ETIMEDOUT`/`ESOCKET`/`ECONNECTION` → "Could not reach the mail server... confirm your hosting provider allows outbound SMTP traffic," `ECONNREFUSED` → refused-connection wording, `EAUTH` → credentials wording, `EDNS` → hostname-resolution wording — reused by the test action, the regular queued-send path, and manual retry, so every failure surface in the app now tells you *which* kind of failure it was instead of a generic message.
3. **No way to test without first saving a password into the database.** Added a secure environment-variable path: `EMAIL_GMAIL_APP_PASSWORD`/`EMAIL_GMAIL_ADDRESS`. When set and Gmail is the selected provider, these are read directly from `process.env` and used to build the transporter — never written to the database, never round-tripped through the Admin UI, never returned to any client. This is the literal "use the project's secure environment/configuration mechanism" the spec asked for, using the project-provided Gmail test account (`letsprintvictoria@gmail.com`) as intended, without the App Password ever touching source code, git, the database, an API response, or a log line.

**Verified live and root-cause-confirmed, not just patched over**: this sandbox's outbound network cannot reach any raw SMTP port at all (confirmed via a direct TCP connection test to `smtp.gmail.com:465`/`:587`, both blocked) — so the real behavior under a genuinely unreachable/blocked SMTP path could be exercised directly. With a (deliberately fake, for this test) App Password supplied via `EMAIL_GMAIL_APP_PASSWORD`, "Test Email Connection" now fails in ~10 seconds with "Could not reach the mail server (ETIMEDOUT)... confirm your hosting provider allows outbound SMTP traffic on that port — some platforms block it" — never an indefinite hang. This can't rule out whether Render's own network specifically blocks SMTP (this sandbox's restriction is a property of the Claude Code environment's proxy, unrelated to Render's), but the fix guarantees that whichever way it goes once real credentials are set on Render, the result will be fast and specific rather than a hang — which is what "must actually work" required.

**Security hardening found and fixed as part of this same pass**: the Email and Messenger Settings pages were passing their full `BusinessSettings` object — including the AES-256-GCM-encrypted password/token *ciphertext* — straight into client-component props, even though the client side only ever checked truthiness (`"•••• (unchanged)"` placeholder logic). The ciphertext therefore rode along in the page's RSC payload/HTML for no reason. Both pages now swap those fields for a plain `"set"`/`null` marker server-side before rendering the form, so no encrypted secret blob reaches the browser at all. Verified via `page.content()` + a regex scan for long base64-like strings on both settings pages — zero matches.

### Login page: added the one genuinely missing tracking entry point

Everything else tracking-related (homepage, `/track/[token]`, reference-number lookup, tracking-link generation on Order/Job Order/Quotation) was already present and working — confirmed via direct file/grep inspection before touching anything. The one real gap was the login page itself having no path to tracking for a customer who doesn't want an account; it now has a "Track Your Order" button (alongside sign-in and the OAuth buttons) linking to the homepage's tracking section, matching the spec's example copy.

### Notification Bell: a real, customer-facing bug

The underlying deep-link mechanism — `notification.link` populated correctly at creation across every `notifyUser`/`notifyCustomer`/`notifyStaff` call site, `openNotificationAction` marking read and redirecting to that link, the floating Chatbox opening directly for message-type notifications via a `chatbox:open-conversation` event rather than an old standalone page — was already correct, and was verified live end-to-end for an Order/Fulfillment notification before any code was touched. One page broke the pattern: **`/soa/view/[id]` required `requireRole(["STAFF","ADMIN"])`**, so a customer clicking their own "Statement of Account Available" notification — which correctly links to `/soa/view/{id}` — was silently redirected to `/dashboard` by that page's own gate before ever seeing the SOA. This is exactly the "clicking a notification just refreshes the page" symptom described, isolated to one specific page rather than the general mechanism.

Fixed by mirroring the identical ownership-check pattern already used by the sibling `/soa/[id]/print` page (which already correctly supported customer access): staff/admin can view any statement (still gated by the existing `SOA_VIEW` permission for STAFF), a customer can view only their own statement, anyone else is redirected to `/payments` with no data exposed. Verified live both directions — the owning customer's SOA notification now opens the real statement (statement number, Total Outstanding both rendered); a different customer hitting the same URL directly is redirected away with zero statement data present in the response.

Also added a global `app/not-found.tsx` — "This item is no longer available," with a link back home — so any stale/deleted/revoked record a notification (or any other link) points to shows a friendly message instead of Next's generic 404, per spec.

### Production Kanban: visual redesign, logic untouched

A UI-only overhaul, verified to leave every underlying rule (`startProduction`/`completeCurrentStage`, permission gates, the WorkflowTemplate/WorkflowStage-driven columns, service-specific stage sequences) exactly as it was:

- **Header**: "Operations & Workflow" eyebrow label, "Printing Production Kanban" title and subtitle, four live stat pills (Active Jobs / In Production / In QC / Ready) computed from the same job-order set the board already renders.
- **Search** (JO number, customer name, service, order number) alongside the existing Service filter, client-side, combinable.
- **Cards** gained: up to two of the Job Order's Service-Master-configured spec highlights, the order total (Staff/Admin only — explicitly hidden for the PRODUCTION role, which has no reason to see financial amounts), the most recent fulfillment's courier when one exists, and a subtle Overdue indicator derived purely from the existing `deadline` field (no fabricated High/Urgent/Normal priority levels — nothing in the schema backs those, so nothing was invented to fill that example).
- **Compact actions**: Details, a document-icon link to the existing Job Order print page, an inline Chat icon (reuses the exact same `openTransactionInChatAction` + `chatbox:open-reference` event the existing "Discuss in Chatbox" button already uses, just sized to fit a card instead of a full card-width button), and "Next →" for stage advancement — the same `completeCurrentStage`/`markStageInProgressAction` actions as before, now with a clearer inline confirm step (notes field + Confirm/Cancel) before the stage actually advances.
- **Empty states**: a small package icon + "No jobs in this queue" instead of blank space.
- **Responsive strategy**: stat pills wrap to a 2-column grid on mobile; a stage-selector dropdown shows one column's cards at a time on narrow screens instead of a horizontally-scrolling row of narrow columns, while tablet/desktop keep the horizontal-scrolling column row.

Kept in the existing Red + White + Montserrat system throughout — no purple/blue/teal from the reference screenshots.

### Verification

A background Playwright pass covered all four areas end to end: both email actions (including the live ~10-second-fail-with-specific-message confirmation described above, and a source-scan confirming no secret ciphertext reaches the browser), the login page's tracking link, a full round-trip on the SOA notification fix (correct customer sees their statement; a different customer is blocked; a stale link shows the friendly not-found page), the full Kanban redesign (header/stats/search/empty-states/mobile-stage-selector/PRODUCTION-role-hides-amounts/Next-confirmation-and-cancel), plus a regression spot-check across the public homepage and every major Staff page. **All 17 checklist items passed — zero real bugs found** this round (a false lead from the verification agent's own test-script selector matching the header's "Sign out" button, causing an unintended logout mid-script, was correctly identified and documented as a test-script issue, not an application defect).

Redeploy-safety re-confirmed after every change in this update: clean git status, zero Prisma migration drift, and `next build` succeeding with Postgres stopped to simulate a Docker build-before-DB-up deploy.

## August 18 — 4th update: Master Transaction / automatic Job Order creation, and the tracking timeline visual fix

Two requirements: (1) a single "Master Transaction / Order Number" that connects Quotation → Job Order → Payment → Invoice without duplicating any of their individual document numbers, plus automatic Job Order creation once a quotation is approved and payment conditions are satisfied, eliminating Staff re-encoding the same information into a second form; (2) fix the broken vertical connector line on the public Tracking Page's progress timeline shown in a customer-supplied screenshot.

Per the spec's own explicit instruction ("before implementing a new Master Transaction model, inspect whether the current system already has an Order, Transaction, Inquiry, or similar parent entity that can serve this purpose"), inspection came first: `Order` already carries a unique `ORD-2026-####` number and already relationally links `Quotation` → `JobOrder[]` → `Payment[]` → `Fulfillment[]`, and "Invoice" in this app has never been a separate model — it's a print view of the Order itself (`/orders/[id]/invoice`). No new top-level model was created; the whole update extends what `Order` already does.

### Automatic Job Order creation (`lib/quotation-conversion.ts`)

- `convertApprovedQuotationToOrder()` — idempotently creates the Order for a just-approved quotation (a no-op if one already exists for that `quotationId`), carrying forward customer, line items, and total with zero re-encoding. Payment terms are derived from the existing `Customer.isQualifiedForTerms` flag — the same approved-terms exception the manual "Create Order" form has always exposed as a Staff-selectable option, just applied automatically — so government/corporate/credit customers are never forced through a payment step, without adding a second payment-rule implementation.
- `autoCreateJobOrderForOrder()` — idempotently creates the Order's first Job Order, carrying forward the quotation's line item (service, specs, quantity, description) and loading that Service's own configured `workflowTemplateId` rather than any hard-coded stage list; skips cleanly (returns a typed reason, doesn't throw) if the line item has no service or the service has no configured production flow. Concurrency-safe by construction: wraps the check-then-create in a `prisma.$transaction` with a Postgres row lock (`SELECT ... FOR UPDATE` on the Order), so repeated approval clicks, a payment-confirm retry racing a page refresh, or genuinely concurrent callers can never create two Job Orders for the same order — verified directly by firing three concurrent `autoCreateJobOrderForOrder()` calls at the same order and confirming exactly one Job Order resulted.
- `convertApprovedQuotation()` — the single entry point both quotation-approval paths call (customer `approveQuotationAction` and staff `forceApproveQuotationAction` in `app/actions/quotations.ts`): creates the Order, then immediately auto-creates the Job Order if `lib/workflow.ts`'s existing `paymentSummary()` (never duplicated, reused as-is) says payment is already satisfied or terms are approved. Otherwise the Order sits with no Job Order until a payment clears — `recordPaymentAction`, `confirmPaymentAction`, and `applyVoucherAction` in `app/actions/payments.ts` all call the same auto-creation check after marking a payment `CONFIRMED`, so a Job Order appears the moment payment conditions are met regardless of which of the three payment paths satisfied them.
- Every automatic creation is audit-logged (`ORDER_CREATED` / `JOB_ORDER_AUTO_CREATED`) with a `trigger` field recording what caused it ("Quotation Approved + No Payment Required", "Quotation Approved + Approved Payment Terms", "Payment Confirmed"), and fires the existing notification system (staff + customer) — no second email/notification mechanism.

### Unified transaction view (`app/(app)/orders/[id]/page.tsx`)

The Order page already fetched `order.quotation` but never rendered it, and already showed payment terms/status, Job Orders, and Fulfillment — the concrete gap was the missing link back to the source Quotation and a consolidated history. Added:
- A **Quotation card** showing the quote number (linked to `/quotations/[id]`), an explicit "Order No.: ORD-2026-####" cross-reference, and the quotation's own status badge.
- A staff-only **Transaction History card** built from the existing `AuditLog` model — entries across the Order, its Quotation, its Job Orders, and its Payments, merged and sorted chronologically — reusing the existing audit trail rather than a new history/timeline system.

Quotation, Job Order, and Invoice pages were checked and already cross-reference the Order Number prominently (a "Linked order" card on the Quotation page, the Order Number directly under the header on the Job Order page, and "Related Order"/Invoice Number fields on the printed Invoice) — no changes needed there. A separate customer-facing "[Track Order]" button was deliberately not added to the authenticated Order page: that page already shows the customer richer information than the public tracking snapshot, so a second tracking view would be exactly the kind of duplicate architecture the spec explicitly warned against.

No `QuotationStatus`/`OrderStatus` enum values were added — "is this order awaiting payment," "does it have a Job Order yet," and "does it have approved terms" are all derivable from existing relations and fields, matching the spec's "reuse the existing status architecture where possible."

### Tracking Page — timeline connector line fix

Root-caused, not hidden: the previous implementation absolutely-positioned the connector line against each `<li>`'s own auto-height, and the "current" step also carried a `border-l-4` directly on that same element — widening that one row and throwing off the line's alignment, which is exactly the visible break shown in the customer's screenshot around Order Received → Quotation → Approved → Job Order → Design. Rewritten in `components/tracking/tracking-snapshot-card.tsx` as a flexbox layout: the icon and connector line live in their own `flex flex-col items-center` column, stretched by the parent row's default `align-items: stretch` to match the content column's real height, with the connector itself `flex-1` — the browser computes each segment's length from actual layout, so it always reaches the next icon regardless of step content height. The current step's border/background emphasis was moved onto the content column only, so it can never again shift the icon/line column's position. Same component, same tracking architecture (`/track/[token]` and the homepage reference lookup both still use it) — no second Tracking Page.

### Verification

- Direct-function test (`tsx`, real Prisma against the seeded DB, cleaned up after): a qualified-for-terms customer's approval created an Order **and** its Job Order immediately with no payment step; a standard customer's approval created only the Order, with the Job Order appearing only after a payment met the 50% partial threshold; repeating the approval call and repeating the payment-triggered creation call were both no-ops (still exactly 1 Order / 1 Job Order); three genuinely concurrent `autoCreateJobOrderForOrder()` calls against the same order still produced exactly one Job Order, confirming the row-lock idempotency guarantee under real concurrency, not just repeated sequential calls.
- Live browser regression pass against seeded data: the Order page's new Quotation card renders correctly with the Order Number cross-reference and zero page errors; the public tracking timeline for a real order (`ORD-2026-0001`, mobile viewport) shows one continuous, correctly-aligned line through Order Received → Quotation → Approved → Job Order → Design (current stage), matching the fix's intent.
- Redeploy-safety: clean git status, zero Prisma migration drift, `next build` succeeding with Postgres stopped to simulate a Docker build-before-DB-up deploy.

## August 18 — 5th update: Messenger Dispatch on the Production Kanban

A Messenger button on each Kanban Job Order card that opens a dispatch dialog for sending a customer-safe production update over Facebook Messenger — built entirely on the Messenger infrastructure from the "Service Master" and "email root-cause" updates (`lib/messenger.ts`'s real Meta Send API path, `MessengerConnection`/`MessengerLog`, the Messenger Settings on/off switch), not a second integration, per the spec's explicit "inspect the existing Kanban and messaging implementation first."

### What was reused vs. added

Reused as-is: tracking-link creation (`generateTrackingLinkAction`), the customer-safe production-progress calculation (`buildOrderTimeline`'s Job-Order-stage loop, extracted into a shared `buildJobOrderStageSteps`/`currentStageLabelForJobOrder` in `lib/order-tracking.ts` so the tracking page and the dispatch message compute "current stage" and "step X of Y" identically from the real Service Production Flow — never a hard-coded stage list), the real Send API path and its MessengerLog trail, and the existing `AuditLog`/permission system. Added: a `sendManualMessengerDispatch()` in `lib/messenger.ts` that sends a Staff-edited message verbatim (the existing `sendMessengerEvent` is template-driven and category-gated, wrong shape for an explicit one-off Staff action); a pure message-generation module `lib/messenger-dispatch.ts`; the dialog's backing actions in `app/actions/messenger-dispatch.ts`; and the dialog UI itself.

### Dialog behavior

Opening the dialog from a Kanban card loads everything in one call — Job Order, Order, Customer, Service, current stage, production progress, tracking-link status, and Messenger connection status — with no separate customer lookup (spec item 24). The generated message follows the spec's example format (business name header, Order Ref, Job Order, Client, Service, Current Stage, Production Progress, order details, tracking link) built from real transaction data only; the body sentence defaults to a dynamic "Your order is currently in the {stage} stage" derived from the Job Order's actual current stage, or Staff can swap in one of eight fixed Quick Message Templates (Production Started/Design Ready/Printing/QC/Packing/Ready for Pickup/Shipped/Delivered) — either way the full text stays in an editable textarea, never locked.

Recipient targeting is deliberately honest rather than mimicking the reference screenshot's two-card mockup: a single customer target shows a truthful **Connected** / **Not Connected** badge from the real `MessengerConnection` state. If no active tracking link exists yet, "Generate Link" creates one inline (reusing the exact same creation path as the Order page) and inserts it into the message; "Show QR Code" renders a client-side QR code (the `qrcode` package) encoding that same tracking URL — nothing else. Four dispatch actions are always available in some combination: **Copy Formatted Text** (clipboard + an audit entry), **Open in Messenger** (opens the customer's actual `m.me` opt-in link, pre-copying the message since Messenger doesn't support pre-filled text), and **Send via Messenger** (only shown when the Page is actually configured and the customer has actually connected — never claims to reach an unconnected customer). A send's real outcome — Sent, Failed, or Skipped — is always recorded before anything is reported back; a failed attempt shows the spec's required friendly copy ("Unable to send Messenger update...") rather than the raw Meta API error, with the real reason kept in the audit trail and Messenger Log for Admin.

A 15-minute duplicate-send guard, keyed to the Job Order + current stage, blocks an accidental repeat "Sent" and surfaces "Send Again" instead; reopening the dialog also shows an informational note if any dispatch (Sent/Copied/Failed) happened recently for the current stage, from the same `AuditLog` query. Because every dispatch is audit-logged against the Job Order, it automatically shows up in the Order page's existing Transaction History card from the 4th update — no separate communication log needed.

### Permission and access

New `MESSENGER_DISPATCH` permission (Communication category, additive migration): ADMIN always allowed, STAFF needs the explicit grant, PRODUCTION never sees the button — customer communication isn't a production-floor duty, mirroring the existing amount-visibility restriction already on this same Kanban page. The internal Chatbox (its own button, unchanged) and Messenger are kept as separate, coexisting channels, per spec.

### Verification

- Live Playwright pass against the seeded DB (Messenger enabled with a fake Page ID/token, one seeded customer marked as connected with a fake PSID): the dialog auto-loaded the correct Job Order/Order/Customer/Service, computed "Pressing — Step 3 of 5" correctly from that Job Order's real `WorkflowTemplate` stages, a Quick Template swap worked, Generate Link + QR code both worked inline, Copy and Send were exercised. Sending genuinely attempted the real Meta Graph API call and — since this sandbox has no route to `graph.facebook.com` — failed for real, which correctly resulted in a `FAILED` status (never a false "Sent") with the friendly fallback message shown and the true error kept server-side.
- Two real issues were caught by this same pass and fixed before commit: the dialog's header badges used blue/purple (the spec explicitly says not to copy the reference screenshot's colors — switched to the existing slate/red brand tones), and a failed send was displaying the raw Meta API error text in the dialog (the spec explicitly says never show raw API errors — switched to the required friendly copy).
- Direct-function test confirmed the duplicate-send guard logic fires correctly against a simulated prior "Sent" audit entry for the same stage, and that the Order page's Transaction History query does pick up Messenger dispatch entries automatically.
- Role-visibility check across all four roles confirmed exactly the intended gating: STAFF with the grant sees the button, STAFF without it doesn't, PRODUCTION never does, ADMIN always does.
- Redeploy-safety: clean git status, zero Prisma migration drift, `next build` succeeding with Postgres stopped to simulate a Docker build-before-DB-up deploy.

## August 18 — 6th update: Google/Facebook customer sign-in — security hardening, Forgot Password, My Profile

Google/Facebook OAuth sign-up already existed from an earlier update — next-auth providers registered only when real credentials are configured, existing-customer linking by email, `passwordHash` nullable for OAuth-only accounts. Per the spec's own "inspect first, do not create duplicate systems," none of that was rebuilt; inspection instead surfaced real gaps against this update's explicit requirements, all fixed or added directly.

### Security gaps closed (`lib/oauth-resolve.ts`, extracted out of `lib/auth.ts` for direct testability)

- **An OAuth sign-in resolved to *any* existing User by email match, regardless of role.** If an Admin's or Staff's login email happened to match what a provider returned, they'd have been signed in *as that administrative account* through Google/Facebook — a real privilege-boundary hole against the spec's explicit "do NOT automatically enable Google/Facebook sign-in for Admin/Owner/Staff" (items 19/20). Social sign-in now only ever resolves to a `CUSTOMER`-role account; a matched non-customer email is rejected outright.
- **No email-verification check before trusting an email enough to link/create an account** (item 10). Google's `email_verified` claim is now checked — an explicit `false` is rejected before any lookup or creation. Facebook's Graph API only ever returns an email for confirmed accounts, so there's no separate flag to check there.
- **No audit trail for social sign-in events** (item 34). The full create-or-link decision moved into NextAuth's `signIn` callback (the only one with access to `profile`, and the one whose return value NextAuth honors for a specific redirect) — every outcome is now `logAudit`-ed: `SOCIAL_LOGIN_SUCCESS`, `SOCIAL_ACCOUNT_CREATED`, or `SOCIAL_LOGIN_FAILED` with a reason, never the raw OAuth token/profile.
- Verified directly against the seeded DB (`lib/oauth-resolve.ts`'s `resolveOAuthUser` is a pure, DB-backed function — no live Google/Facebook handshake needed to test it): a brand-new email creates a `CUSTOMER`; an email matching a pre-existing login-free Customer (created by Staff/Admin, per items 8/9/37) links to that *same* Customer id rather than duplicating it; a second provider signing in with the same email links to the *same* User rather than creating a second one (item 11's "multiple login methods, one account," item 38's new-customer path); a matched Staff email and a matched Admin email are both rejected (`non_customer`); an explicitly-unverified Google email is rejected before any row is written; a missing email is rejected. All 7 scenarios passed.

### Forgot Password (didn't exist — spec assumed it did)

Items 15/16/35 all reference a working "Forgot Password?" link as part of the target login experience, but no such flow existed in the codebase. Built minimally: a `PasswordResetToken` model (1-hour, single-use), `/forgot-password` (always shows the same generic "If an account exists..." message whether or not the email matches, so the form can't be used to enumerate registered emails) and `/reset-password/[token]`, reusing the existing Admin-editable email template system (a new `PASSWORD_RESET` event) rather than a separate send path. Role-agnostic — works for any email/password-capable account, not customer-only, matching the "must continue working" framing. The same flow doubles as "set your first password" for an OAuth-only account (item 14): setting a new password via a valid reset link works identically whether or not one already existed. Both pages added to `proxy.ts`'s public-path allowlist (the token *is* the authorization, same shape as `/track` and `/documents`) — without this fix they'd have been incorrectly redirected to `/login` before ever rendering.

### Return-to-intended-page after login (item 33) — found half-built, finished

`proxy.ts` already set `?callbackUrl=` when redirecting an unauthenticated visit to a protected page, but nothing ever read it back — both `loginAction` and `oauthSignInAction` hardcoded `redirectTo: "/"`. Threaded `callbackUrl` through credentials login, OAuth sign-in, and registration, validated through a same-origin-only `safeRedirectPath()` check (rejects protocol-relative/absolute URLs — open-redirect prevention) before ever handing it to `signIn`. Also carried across the Login ↔ Sign Up cross-links so switching between them mid-flow doesn't lose the intended destination.

### UI (`components/auth/oauth-buttons.tsx`, login/register pages)

Real inline Google "G" and Facebook "f" brand-color icons (not distorted/unofficial marks) replace the previous plain text-only buttons; each provider button is wrapped so `useFormStatus()` gives it its own "Connecting to Google..." pending state and disables during submission (blocks a double-click from firing two auth requests — item 32). The login page was reordered to form-first → OR → Google/Facebook → Forgot password? → Sign Up, matching the spec's explicit final wireframe exactly (it previously showed OAuth buttons *before* the form); the register page's OAuth-first ordering was already correct. Added the register form's missing Confirm Password field (item 36) with a matching Zod refinement. Raw NextAuth error codes (`AccessDenied`, `OAuthCallback`, etc.) plus the new custom rejection reasons are now mapped through `lib/auth-errors.ts` to professional copy — never a raw code shown to a user (items 24/26). Kept entirely in the app's existing Red + White + Montserrat system and the shared branded `(auth)` layout (already mobile-responsive from an earlier update) — no Spotify black/green anywhere, per the explicit "reference for layout concept only" instruction.

### My Profile + Login & Security (new `/account/profile`, linked from the customer sidebar nav)

Didn't exist at all (items 12/28). Self-service edit of Full Name/Company/Email/Contact Number/Address/Facebook via a new `updateOwnProfileAction` — deliberately edits `Customer.email` (contact email) only, never `User.email` (the login identity OAuth matching relies on), so this form can't accidentally break account linking. A Login & Security card shows which methods are connected (Email/Password, Google, Facebook) using two new `User` columns (`googleConnectedAt`/`facebookConnectedAt`) set on every successful resolution — read-only status only; unlinking wasn't built, since the spec explicitly hedges it as optional and warns against an accidental lockout, so leaving it out entirely avoids that risk without contradicting the spec. For an OAuth-only account, a "Set a Password" form (no old password required, since none exists) appears — the concrete version of item 14. `Customer.profileImageUrl`, populated from the OAuth provider's photo on first sign-up (only if not already set), displays on this page only — the business logo still owns login/sidebar branding everywhere else, per item 13's explicit constraint.

### Verification

- All 7 `resolveOAuthUser` scenarios above, direct-function against the seeded DB.
- Live Playwright pass: generated a real reset token for a test OAuth-only account, completed `/reset-password/[token]` through the actual UI (reachable while logged out, confirming the proxy.ts fix), landed signed-in automatically, and confirmed `/account/profile` correctly showed **both** Email/Password and Google as Connected on the same account — a live, visual confirmation of the "multiple login methods, one customer" requirement (items 11/37/38), not just a DB-level check. `/forgot-password` showed the identical generic message for a nonexistent email. Register correctly rejected a Confirm-Password mismatch. Admin login (a non-customer, non-OAuth path) confirmed unaffected. Zero page errors across every check.
- Filled a real deployment-config gap while verifying: `.env.example` had never documented `GOOGLE_CLIENT_ID`/`SECRET` or `FACEBOOK_CLIENT_ID`/`SECRET` at all (item 22/23's "environment-based, per-environment credentials, documented setup") — added, with redirect-URI and per-environment-credentials guidance, never real values.
- Redeploy-safety: a full fresh-database migrate (14 migrations, including this update's) plus reseed succeeded cleanly, zero Prisma migration drift, `next build` succeeding with Postgres stopped to simulate a Docker build-before-DB-up deploy.

### Correction: an honest scope note, "Connect" support, and the LBC-style Messenger follow-up

The user correctly pushed back on the verification above: it confirms the *internal resolution logic* (`resolveOAuthUser`) against the real database, not a live handshake with Google or Facebook — this sandbox has never had real OAuth credentials, and its outbound network is policy-blocked to `facebook.com`/`graph.facebook.com` entirely (confirmed directly via the proxy status, not assumed), so a true end-to-end provider round-trip cannot be self-certified from here regardless of credentials. Per the user's direction, real-credential wiring and the live click-through are deferred to the user's own testing (checklist delivered separately covering Sign Up/Sign In with both providers, the existing-customer-linking scenario, and cancellation) — the work below stayed scoped to closing genuine code-level gaps the correction surfaced, not to faking a verification that didn't happen.

**"Connect Google/Facebook from your account" — a real missing feature, not just untested.** The original implementation only ever handled fresh sign-in/sign-up; an already-logged-in customer (via password or the other provider) had no way to attach an additional provider to their existing account. Added "Connect Google"/"Connect Facebook" buttons to My Profile's Login & Security card (shown only for a configured, not-yet-connected provider) backed by a new `initiateConnectAction`, which marks intent via a short-lived httpOnly cookie before the normal OAuth redirect. `lib/auth.ts`'s `signIn` callback checks that cookie and — before any database write — refuses to proceed unless the provider's returned email matches the already-logged-in user exactly; on a mismatch, NextAuth never establishes the new session (the original one is left completely untouched) and a friendly "that account doesn't match" error shows on the profile page. This was the one place a naive implementation could have silently created a duplicate customer or swapped a browser's session to an unrelated account, so it got the same amount of care as the sign-in-side security fixes.

**LBC Track & Trace-style "Follow via Messenger."** Added a "Follow Order Updates" section to the shared `TrackingSnapshotCard` (covers both `/track/[token]` and the homepage reference lookup in one change) — "Would you like to receive updates for this order through Messenger?" with a Follow via Messenger button. Uses the exact same real `m.me` opt-in deep link (`messengerOptinLink`) already built for the customer dashboard's "Connect Messenger" card — not a second integration — computed server-side in `buildPublicSnapshot` and returned only as the link itself (never the customer's internal id), null (and the whole section hidden) whenever Messenger isn't enabled/configured. Kept in the app's Red + White + Montserrat system; LBC was a layout/UX reference only, and the earlier Spotify reference is no longer used anywhere.

**Verification, scoped honestly**: all 7 `resolveOAuthUser` scenarios re-confirmed after this round of changes; the Connect flow's mismatch-guard logic was implemented and reasoned through carefully but — like the rest of the live-provider surface — cannot be exercised end-to-end without real credentials, so it's included in the checklist handed to the user rather than claimed as verified here. The Follow via Messenger addition *was* fully verified live (it needs no third-party credentials): both tracking surfaces show the section with a working `m.me` link when Messenger is configured, and correctly hide it when disabled. Redeploy-safety re-confirmed: clean git status, zero Prisma migration drift, `next build` succeeding with Postgres stopped.

### Follow-up: Admin Authentication Settings page (in-app OAuth credentials)

Google/Facebook credentials worked only via environment variables up to this point — asked directly whether an in-app settings page existed instead (there wasn't one), matching how Email and Messenger already work. Built `/admin/auth-settings` on that exact pattern: Client ID/Secret fields for both providers, the masked `•••• (unchanged)` convention for secrets (never re-displayed, never sent to the client — same discipline as the Email/Messenger settings pages), redirect-URI reference text, and a status badge per provider distinguishing "Configured (Settings)" / "Configured (Environment)" / "Not Configured". New `BusinessSettings` columns (additive migration): `googleClientId`, `googleClientSecretEnc`, `facebookClientId`, `facebookClientSecretEnc`, secrets encrypted with the same AES-256-GCM helper as the SMTP password and Messenger Page token.

The real work was underneath: `lib/auth.ts` previously built its Google/Facebook provider list once, synchronously, from `process.env` at module load — there was no way to read anything from the database into it. Rewrote it to use NextAuth's own supported "advanced initialization" form, `NextAuth(async (req) => {...})`, evaluated fresh per request, which is what makes a no-redeploy-needed, database-backed credential change possible at all. Database-configured credentials take precedence when set; the environment variables remain a valid fallback for deployments that would rather not store OAuth secrets in the database. `lib/oauth-providers.ts`'s `availableOAuthProviders()` (which decides whether the login/register pages even show the buttons) now mirrors that exact same precedence, so it can never advertise a button that would actually fail.

This was the highest-risk change in the whole update — it restructures the file that gates login for every role, not just customers — so it was smoke-tested immediately after the rewrite, before building anything else on top of it: a real `admin@lp.test` credentials login, confirmed via an explicit navigation to a protected page afterward (not just the click-through redirect, which this sandbox's Playwright setup sometimes reports unreliably) that the session cookie was genuinely established.

**Verification**: saved fake Google/Facebook credentials through the actual Admin Settings UI — the status badges correctly flipped to "Configured (Settings)", the login and register pages correctly rendered the Google/Facebook buttons with **zero environment variables set**, and a raw-HTML scan confirmed neither secret ever reached the page. Then, because this touched core auth, re-ran the full login regression across all four roles — Admin, Staff, Production, Customer — each confirmed to still authenticate correctly with plain email/password after the rewrite. Redeploy-safety re-confirmed: zero Prisma migration drift, `next build` succeeding with Postgres stopped.
